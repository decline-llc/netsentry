name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rc-check:
    name: Release candidate checks
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version-file: engine/go.mod
          cache-dependency-path: engine/go.sum

      - name: Install native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential gcc make libpcap-dev python3 curl

      - name: Install pinned supply-chain tools
        run: |
          cd engine
          go install golang.org/x/vuln/cmd/govulncheck@v1.6.0
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12

      - name: Run supply-chain checks
        env:
          SUPPLY_CHAIN_FETCH_ASSETS: "1"
        run: make supply-chain-check

      - name: Run release-candidate checks
        env:
          DOCKER: docker
          IMAGE: netsentry:ci-${{ github.run_id }}
          VERSION: 0.1.0-ci.${{ github.run_number }}
        run: make rc-check
