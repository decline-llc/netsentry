# Task Plan: reconcile release readiness after pushed commit

## Metadata

- Timestamp: 20260708-175131
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: ENV-01, ENV-02, ENV-03, DOC-01, DOC-02, QUALITY-03, DEPS-01, SKILL-01, SKILL-02

## Background

The latest release validation recovery commit is already present at `origin/main`.
Release-readiness documentation still lists pushing a passing release commit as a blocker, which is now stale. The remaining pre-tag blockers should focus on evidence that has not been recorded and the future version-tag publication verification.

## Goal

Reconcile public release-readiness documentation so it accurately reflects that the release commit is pushed, while keeping the real pre-tag blockers visible.

## Scope

- Update release-readiness blocker wording.
- Update README release blocker wording.
- Add this task plan for traceability.

## Non-Goals

- Do not create or push a version tag.
- Do not publish a GitHub Release or GHCR image.
- Do not change runtime code, API behavior, configuration, CI workflow behavior, or dependencies.

## Files to Inspect

- `docs/release-readiness.md`
- `README.md`
- `docs/tasks/task-state-20260708-051113.json`
- Git remote and branch state

## Files to Modify

- `docs/release-readiness.md`
- `README.md`
- `docs/plans/task-20260708-175131.md`

## Docs to Update

- `docs/release-readiness.md`
- `README.md`

## Dependency Impact

No dependency changes are expected. `go.mod` and `go.sum` must remain unchanged.

## Performance Impact

No runtime or release-workflow performance impact.

## Quality Gates

- `make docs-check`
- `git diff --check`
- Confirm no dependency file changes.

## Execution Steps

1. Confirm local branch and remote sync state.
2. Update stale blocker wording in release-readiness docs.
3. Run documentation and diff checks.
4. Commit the documentation-only change atomically.
5. Push the commit to the approved upstream repository if validation passes.

## Validation Plan

Run `make docs-check`, `git diff --check`, `git status --short --branch`, and `git rev-list --left-right --count origin/main...HEAD`. Inspect `git diff --stat` to confirm only documentation/plan files changed.

## Risk Points

- Accidentally implying v0.1.0 has already been tagged or published.
- Hiding the still-pending external fuzz and realistic corpus evidence gates.
- Pushing through a remote URL that does not match the approved repository.

## Rollback Plan

Revert the documentation commit if wording is wrong or if release ownership requires the old blocker phrasing.

## Acceptance Criteria

- Public docs no longer list the already-pushed release commit as pending.
- Public docs still require external fuzz evidence, realistic sanitized corpus evidence, and successful tag-driven publication verification before v0.1.0 is considered released.
- Validation commands pass.
- One documentation-only commit is pushed to the approved repository.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Created a measurable goal for reconciling release-readiness documentation and validating the documentation-only change. | None |

## Validation Result

- `make docs-check`: passed.
- `git diff --check`: passed.
- Dependency file diff check: no `go.mod`, `go.sum`, `engine/go.mod`, or `engine/go.sum` changes.
- Sensitive keyword scan over staged diff: no matches.
- Remote read check: `git ls-remote --heads origin main` passed.

## Push Status

- `git push origin main`: blocked because the current environment has no non-interactive GitHub HTTPS write credentials.
- Failure details recorded in `docs/errors/git-push-fail-20260708-175343.log`.
- Superseding correction: the project already had persisted task history requiring SSH remotes after user clarification, so `origin` was restored to `git@github.com:decline-llc/netsentry.git`.
- `ssh -T git@github.com` authenticated successfully as `decline-llc`; final push should use SSH.
