# Task Plan: run large repeat-pcap pressure check

## Metadata

- Timestamp: 20260706-233205
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: native Makefile, v0.1.0 boundary, docs sync, dependency check, quality gate, sensitive-info scan

## Background

The v0.1.0 release readiness checklist still requires realistic pcap corpus pressure and query evidence. The repository does not currently include or reference a real external pcap corpus, and production traffic samples must not be committed without sanitization. The existing `make e2e-pressure` repeat-pcap harness is synthetic, but it verifies the full C capture -> UDS -> Go receiver -> rule worker -> SQLite aggregation -> API health/alerts path at a larger local size.

## Goal

Run `PRESSURE_REPEATS=10000 make e2e-pressure` and record the current release candidate's 60,000-packet synthetic repeat-pcap pressure result, while explicitly leaving the realistic external corpus blocker open.

## Scope

- Run the existing repeat-pcap e2e pressure target at 10,000 repeats.
- If the larger run exposes an undersized validation wait window, make only the minimal harness change required to let the worker and SQLite aggregation drain.
- Record packets, raw alerts, aggregated rows, elapsed time, packet rate, and alert rate.
- Record results in this plan and ignored local `DEVLOG.md`.
- Keep realistic pcap corpus pressure/query evidence documented as pending.

## Non-Goals

- No engine, capture, rule, storage, or API runtime code changes.
- No new corpus files.
- No Docker execution.
- No claim that synthetic repeat-pcap replaces realistic corpus testing.

## Files to Inspect

- `scripts/e2e_pressure.sh`
- `README.md`
- `docs/development.md`
- `scripts/gen_test_pcap.py`
- `docs/performance.md`
- `docs/development.md`
- `DEVLOG.md`

## Files to Modify

- `scripts/e2e_pressure.sh`
- `README.md`
- `docs/development.md`
- `docs/performance.md`
- `docs/plans/task-20260706-233205.md`
- `DEVLOG.md` (ignored local state file)

## Docs to Update

- This task plan.
- README, development guide, and performance notes if the pressure wait becomes configurable.
- Local `DEVLOG.md` validation record.

## Dependency Impact

No dependency changes. Run `make deps-check` to verify current Go module integrity.

## Performance Impact

No runtime behavior changes. The pressure command builds binaries and runs a local synthetic end-to-end pressure workload.

## Quality Gates

- `PRESSURE_REPEATS=10000 make e2e-pressure`
- `bash -n scripts/e2e_pressure.sh`
- `git diff --check`
- `make deps-check`
- `make lint`

## Execution Steps

1. Confirm the working tree is clean except ignored local `DEVLOG.md`.
2. Run the 10,000-repeat pressure target.
3. If sandbox socket restrictions block the run, rerun with elevated execution and record it.
4. Record metrics and explicitly keep realistic corpus validation pending.
5. Run lightweight validation and sensitive-info scan.
6. Commit and push the tracked validation plan.

## Validation Plan

Primary validation is `PRESSURE_REPEATS=10000 make e2e-pressure`, which should verify 60,000 packets received/processed, 50,000 raw alerts generated, five aggregated rows, zero decode errors, zero alert write errors, and matching aggregate counts. If local load requires it, rerun with a documented `PRESSURE_WAIT_ATTEMPTS` value.

## Risk Points

- Unix socket operations may fail under sandbox restrictions.
- Results are local and synthetic; do not present packet rate as a production PPS guarantee.
- Temporary pcap, SQLite, socket, and config files must remain outside tracked paths.

## Rollback Plan

If validation cannot run, record the blocker in this plan. If the plan should not be committed, delete this file. No source rollback is expected.

## Acceptance Criteria

- The 10,000-repeat pressure target passes, or a concrete blocker is recorded.
- The plan records packet count, alert count, aggregated row count, elapsed time, and rates.
- Realistic external pcap corpus evidence remains documented as pending.
- No engine/capture runtime source or dependency changes are made.

## Validation Result

- Initial sandboxed `PRESSURE_REPEATS=10000 make e2e-pressure`: failed before test execution because local socket creation was denied with `PermissionError: [Errno 1] Operation not permitted`.
- Elevated `PRESSURE_REPEATS=10000 make e2e-pressure` with the previous fixed 200-iteration post-capture wait exposed a pressure harness timing issue: after capture exited, worker/SQLite drain had not reached the expected totals before final validation.
- Updated the harness to support `PRESSURE_WAIT_ATTEMPTS`, defaulting to `1200`, so larger local runs can wait longer for worker and SQLite aggregation drain without changing engine behavior.
- `bash -n scripts/e2e_pressure.sh`: passed.
- Elevated `PRESSURE_REPEATS=10000 make e2e-pressure`: passed.
- Result: `packets=60000 alerts=50000 aggregated_rows=5 elapsed_sec=108.615 pps=552 alerts_per_sec=460`.
- `git diff --check`: passed.
- `make shell-check`: passed.
- `make docs-check`: passed.
- `make deps-check`: passed, `go mod verify` reported all modules verified.
- `make lint`: passed; `staticcheck` was not installed and was skipped by the Makefile.
- This is synthetic repeat-pcap evidence only. Realistic external pcap corpus pressure and query evidence remains pending.

## Security Check

No key, token, private key, sudo password, production secret, dependency, Docker, or runtime engine change was added. Sensitive-pattern scan only matched the existing `${NETSENTRY_API_TOKEN:}` documentation placeholder and an empty `api_auth_token: ""` test config field in `scripts/e2e_pressure.sh`. Temporary pcap, SQLite, config, and socket files were created under `/tmp` by the pressure script and cleaned up by its trap.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Applied | Goal narrowed to recording a 60,000-packet synthetic repeat-pcap pressure result without claiming to close realistic corpus validation. | Not needed |
