# Task Plan: R90-04 public real-traffic corpus evidence

## Metadata

- Timestamp: 2026-07-16T08:50:44Z
- Branch: main
- Risk Level: Medium
- Remote baseline: `6803f063e101c0e733c99dc79b95f125851540f2`

## Goal

Validate NetSentry against one public, anonymized, real-traffic PCAP under the
approved R90-04 exception, while recording reproducible, path-redacted review
and pressure-test evidence.

## Scope

- Obtain MAWI samplepoint-B trace `200012281400`, a publicly released WIDE
  backbone trace.
- Verify its provenance and the archive's documented anonymization, inspect its
  metadata for sensitive content, then apply the repository sanitizer to a
  local copy outside Git.
- Run the corpus-pressure target against only that sanitized copy and record
  non-sensitive result metrics, review decisions, and the source checksum.
- Update the roadmap and R90-04 task state with verified delivery evidence.

## Non-Goals

- Do not commit PCAPs, local paths, packet payloads, operator details, or raw
  local evidence.
- Do not use synthetic or generated traffic, claim release approval, tag,
  publish, or begin R90-05.
- Do not treat this public-traffic exception as satisfying later
  production-derived evidence requirements.

## Risks and Controls

- Public captures can still contain sensitive metadata. Use the MAWI
  anonymization/payload-removal documentation, review capture headers and a
  bounded packet sample, re-sanitize locally, and retain only redacted summary
  metrics in Git.
- The source is historical. Record exact archive ID, download URL, ETag-derived
  checksum, and documented start/end time; this increment validates a public
  real-traffic input, not current production traffic.
- Corpus-pressure processing may expose parser or service failures. Treat any
  nonzero parse, drop, UDS, or query result as a blocker and do not commit
  approved evidence.

## Acceptance Criteria and Validation

- Provenance, privacy, sanitization, and sensitive-metadata controls each have
  explicit, evidence-based approval in a public record.
- `PCAP_CORPUS=<sanitized local copy> make e2e-corpus-pressure` passes with
  redacted output and a successful query.
- Focused release-gate tests, documentation checks, knowledge check, and
  `git diff --check` pass before commit.
- The resulting commit is pushed, fetched back as `origin/main`, then synced to
  the local Vault using the exact pushed range.

## Stop Conditions

Stop without committing approved evidence if the download cannot be verified,
the privacy review is ambiguous, sanitization fails, corpus-pressure reports a
failure, or a step requires release authority, private traffic, tagging, or
publication.

## Verified Delivery

- Feature commit: `009b2a03776987359661c4ab2776f5d04820db34`.
- Remote verification: fetched `origin/main` matched the feature commit and the
  checked-out `main` branch.
- Post-fetch validation: `make knowledge-check` passed.
- Vault synchronization: exact range
  `6803f063e101c0e733c99dc79b95f125851540f2..009b2a03776987359661c4ab2776f5d04820db34`
  produced the iteration note, full-index entry, and `00-MOC` link.
- Next increment: R90-05 remains pending until its 2026-09-12 window begins.
