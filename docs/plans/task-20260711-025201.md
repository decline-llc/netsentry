# Task Plan: knowledge-governance-closure

## Metadata

- Timestamp: 2026-07-10T19:52:01-07:00
- Branch: main
- Risk Level: Medium
- Required Skill: define-goal; security-best-practices policy review; yeet for commit/push
- Inherited Policies: DEP-03, DEP-04, DEP-05, DEP-06, DEP-07, DEP-08, DEP-11, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18, DEP-19

## Background

The knowledge vault, project knowledge-management policy, and post-push hook were created in the prior implementation pass. The repository still has the policy document uncommitted. This task closes the governance loop under `.codex/system_prompt.md` without changing the C capture protocol, UDS frame contract, SQLite schema, or HTTP response shapes.

## Goal

Commit and push the knowledge-management governance artifacts as one auditable docs topic after repository, secret, documentation, native Makefile, Go, API smoke, and dependency checks pass.

## Scope

- Keep `/home/virtual-machine/Desktop/NetSentry-Knowledge` as the exclusive knowledge-content location.
- Version `docs/knowledge-management.md`.
- Preserve and validate `.git/hooks/post-push` as local Git configuration.
- Add this plan and task state required by the governance prompt.
- Run existing checks and record unavailable gates as not applicable.

## Non-Goals

- No changes to C parser, UDS protocol, Go engine, SQLite schema, API contract, runtime configuration, or dependencies.
- No Docker operation unless an existing gate requires it; Docker is not needed for this docs-only change.
- No new third-party tool or build pipeline.

## Files to Inspect

- `.codex/system_prompt.md`
- `docs/knowledge-management.md`
- `.git/hooks/post-push`
- `Makefile`, `README.md`, `docs/release-readiness.md`
- Git status, remote, branch and recent history

## Files to Modify

- `docs/knowledge-management.md` (already present; verify only)
- `docs/plans/task-20260711-025201.md`
- `docs/tasks/task-state-20260711-025201.json`

## Docs to Update

- `docs/knowledge-management.md` is the implementation artifact and already documents the hook, vault, links, version sync, and manual command.
- README/API/config docs: not applicable; no behavior, API, config, command, dependency, or deployment change.

## Dependency Impact

None. No `go.mod`, `go.sum`, C link flags, Docker image, or package dependency changes. Run `make deps-check`; `go mod tidy` is not required because dependencies are unchanged.

## Performance Impact

None at runtime. The hook performs bounded Git metadata extraction and writes only to the external vault after push.

## Quality Gates

- `git diff --check`
- `bash -n .git/hooks/post-push`
- vault YAML/link/required-section audit
- `make docs-check`
- `make shell-check`
- `make deps-check`
- `make test-coverage`
- `make e2e-smoke`
- `cd engine && GOCACHE=/tmp/netsentry-go-cache go test ./...`
- `make lint` (record project target behavior)
- API/monitoring baseline via e2e health/metrics assertions

## Execution Steps

1. Confirm repository, SSH remote, branch, worktree and secret scan baseline.
2. Persist this plan and the task state.
3. Validate docs, vault, hook, Makefile gates and runtime smoke behavior.
4. Review diff/status and verify only this docs governance topic is included.
5. Commit with `docs: 完善知识管理治理闭环`.
6. Push `main` to the verified SSH `origin` using the approved yeet flow.
7. Record commit/push result and final risks in task state and report.

## Validation Plan

The task passes only if all applicable checks return zero, `make e2e-smoke` reports the expected deterministic alerts, the vault audit finds no malformed note, and `git remote get-url origin` equals `git@github.com:decline-llc/netsentry.git`.

## Risk Points

- The original knowledge-vault isolation requirement must remain intact.
- The hook is outside Git's normal worktree status and must be checked directly.
- `make test-coverage`/`make e2e-smoke` may create ignored local evidence; no generated evidence may be staged.
- A push conflict or remote change must stop the flow and create a conflict log rather than be resolved blindly.

## Rollback Plan

Before push, remove only the task's staged commit with a non-destructive corrective commit if necessary. Do not reset unrelated user changes. If push fails because origin is ahead, fetch/rebase per policy and stop for manual confirmation if a conflict touches core files.

## Acceptance Criteria

- Plan and state files exist and are complete.
- Knowledge policy, hook and vault remain valid and isolated.
- All applicable validation commands pass.
- No secret is found in changed/staged content.
- One compliant docs commit is created and pushed to the verified SSH remote.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Success | Goal refined and created with measurable validation and scope boundaries | None |
| security-best-practices | Not applicable | No security implementation or security review requested; passive policy checks retained | General secret scan and project security policy checks |
| yeet | Fallback | `gh` is not installed, so PR automation is unavailable; native Git will perform the required atomic SSH commit/push | Manual `git add`/`git commit`/`git push`, no PR creation |

## Skill Fallback Record

| Skill | Failure Type | Time | Fallback Template | Impact | Final Decision |
|---|---|---|---|---|---|
| define-goal locator | Initial path mismatch (`.system/define-goal` absent) | 2026-07-10 | Re-locate under `/home/virtual-machine/.codex/skills/define-goal/SKILL.md` | None; authoritative skill was read fully | Continue with located skill |
| yeet | Missing prerequisite (`gh` command) | 2026-07-10 | Fallback Plan: preserve scope, stage only planned docs, run staged secret/diff checks, native atomic commit and SSH push; stop on push conflict | PR creation unavailable; commit/push audit remains possible | Continue with manual Git fallback |

## Fallback Plan

### Reason for Fallback

The `yeet` skill requires the GitHub CLI, but `gh --version` failed because `gh` is not installed.

### Simplified Goal

Complete the required single-topic docs commit and SSH push without opening a PR.

### Minimal Scope

Only `docs/knowledge-management.md`, this plan, and the task state are staged.

### Safety Constraints

No core module, protocol, schema, API, dependency, or generated local evidence is staged. Recheck remote URL and staged content before commit and push.

### Validation Required

Retain all completed Makefile, Go, vault, hook, diff, and secret checks; run staged `git diff --check` before commit and verify the pushed SHA afterward.

### Risk Upgrade

No risk upgrade: PR automation is unavailable, but repository commit/push safety is preserved.

### Stop Condition

Stop immediately on remote mismatch, authentication failure, conflict, or any new validation failure; create the required error/conflict record before further action.
