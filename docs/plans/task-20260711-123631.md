# Task Plan: verify v0.1.0 GHCR publication

## Metadata

- Timestamp: 2026-07-11T12:36:31Z
- Branch: main
- Risk Level: Medium
- Required Skill: define-goal
- Inherited Policies: ENV-01, ENV-02, ENV-03, SKILL-01, SKILL-02, DOC-01, DOC-02, QUALITY-01, QUALITY-03, DEPS-03, VAL-07, VAL-08

## Background

The signed `v0.1.0` tag and public GitHub release have already been verified. The persisted release state records one remaining item: verify the tag-triggered Docker Publish workflow and the `ghcr.io/decline-llc/netsentry:v0.1.0` image with authenticated access. GitHub CLI authentication is available and uses the repository's SSH Git protocol.

## Goal

Obtain reproducible, authenticated, read-only evidence of the v0.1.0 Docker publication result without modifying the tag, workflow, package, release, repository contents, or remote configuration. Completion requires a GitHub Actions run conclusion plus a registry/package manifest result; if authorization or visibility blocks either result, record the exact blocker and stop.

## Scope

- Inspect the release workflow definition and tag-triggered workflow runs.
- Query the package/registry with the existing authenticated GitHub CLI credentials.
- Verify that the remote `v0.1.0` tag still resolves to the locally verified commit.
- Persist only the planning and task-state evidence records when the result is established.

## Non-Goals

- Do not rerun, edit, cancel, or dispatch workflows.
- Do not create, delete, retag, overwrite, or publish images.
- Do not recreate, force-move, or push the `v0.1.0` tag.
- Do not change application code, dependencies, release artifacts, or Git remotes.

## Files to Inspect

- `.github/workflows/docker-publish.yml`
- `.github/workflows/release.yml`
- `docs/evidence/release-v0.1.0.md`
- `docs/tasks/task-state-20260711-044844.json`
- GitHub Actions run metadata and GHCR package metadata

## Files to Modify

- `docs/plans/task-20260711-123631.md`
- `docs/tasks/task-state-20260711-044844.json` only if verification reaches a definitive result
- `docs/evidence/release-v0.1.0.md` only if verification reaches a definitive result

## Docs to Update

Update the release evidence and task state only with verified facts. No application documentation changes are expected.

## Dependency Impact

None. The existing GitHub CLI is used for read-only remote inspection; no package or module changes are permitted.

## Performance Impact

None.

## Quality Gates

- Confirm `gh auth status` has the required authenticated package/workflow scopes without recording credentials.
- Verify the remote annotated tag commit remains `32aafafc8b0d7cadbc53499a1b70d68a5e07ccc1`.
- Inspect workflow run conclusion, event, head SHA, and relevant job conclusions.
- Query authenticated GHCR/package metadata and, where available, retrieve the `v0.1.0` manifest digest.
- Run `make deps-check`, `git diff --check`, and a sensitive-information/absolute-path review of changed docs.

## Execution Steps

1. Persist this plan and validate the repository, SSH remote, authenticated CLI session, and remote tag SHA.
2. Read the Docker publication workflow to determine the expected trigger and image tags.
3. Use authenticated, read-only GitHub CLI/API requests to locate the tag-triggered run and its job conclusions.
4. Use authenticated, read-only package/registry requests to verify the `0.1.0` image tag and manifest if visible.
5. Update evidence/state only with directly observed outcomes; otherwise record the authorization or visibility blocker.
6. Run the applicable documentation, dependency, diff, and sensitive-information checks; deliver the completed verification as an isolated commit and push, then synchronize the local Vault only after the push is verified.

## Validation Plan

The verification passes if the Docker Publish workflow for `v0.1.0` completed successfully against the immutable tag commit and GHCR returns package/manifest metadata for the `0.1.0` tag. A workflow success with an inaccessible package is a partial result, not a publication claim. An authentication or visibility denial is recorded as a blocker, with no remediation performed in this task.

## Risk Points

- Exposing a GitHub token or credential in logs.
- Treating a successful workflow as proof of public image availability without a registry query.
- Mutating historical release objects during verification.
- Recording package paths, private metadata, or absolute local paths in committed documentation.

## Rollback Plan

The only planned writes are documentation records. Revert the evidence/state documentation commit as one atomic change if an observed result was recorded incorrectly. Never alter the existing release tag as rollback.

## Acceptance Criteria

- The remote tag remains unchanged and resolves to the verified release commit.
- Authenticated workflow evidence is recorded with event, SHA, and conclusion.
- Authenticated GHCR/package evidence is recorded with tag and manifest/package result, or a specific blocker is documented.
- No release, workflow, image, dependency, or source mutation occurs.
- Applicable checks pass, and the verification records are committed and pushed as one isolated documentation increment.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Success | Created a bounded, evidence-based read-only publication-verification objective with explicit stop conditions. | None |

## Completion Record

- GitHub CLI authentication was present with `repo`, `workflow`, and `write:packages` scopes; no credential value was recorded.
- The remote `v0.1.0` tag remains at `32aafafc8b0d7cadbc53499a1b70d68a5e07ccc1`.
- Tag-triggered Docker Publish [run 29140426396](https://github.com/decline-llc/netsentry/actions/runs/29140426396) was a successful `push` event for `v0.1.0`, with the matching head SHA and a successful `Build and publish image` job.
- Authenticated package metadata confirms a public `decline-llc/netsentry` container package. Public registry inspection confirms `ghcr.io/decline-llc/netsentry:v0.1.0` has OCI index digest `sha256:efd9975e52a54e41d1293d157c9d170f1af7aa499439dd76a5c317f72d133052` and a `linux/amd64` manifest.
- A separate manual run 29145146270 successfully published `0.1.0` from main commit `938c31a`; this is recorded only to prevent source confusion and is not release-tag evidence.
- No remote release, workflow, package, image, tag, or repository configuration was modified.
