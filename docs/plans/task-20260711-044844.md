# Task Plan: approved v0.1.0 release exception

## Metadata

- Timestamp: 2026-07-11T04:48:44Z
- Branch: main
- Risk Level: High
- Required Skill: define-goal
- Inherited Policies: DEP-03, DEP-04, DEP-05, DEP-06, DEP-07, DEP-08, DEP-10, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18, DEP-20
- Approval: user explicit approval in conversation

## Background

The release gate has complete synthetic fuzz and corpus-pressure evidence but no internal production-derived pcap corpus. The user explicitly approved a version-scoped exception that skips only the real-production pcap evidence requirement and expires before v0.1.1.

## Goal

Record the approved exception, make `release-gate` validate it fail-closed, preserve all other fuzz/synthetic-pressure/sensitive-information checks, then complete the v0.1.0 tag workflow only after local validation passes.

## Scope

- Add the version-scoped exception audit metadata.
- Add strict standard-library parsing for the exception file.
- Add a reviewed public evidence record that labels the pcap evidence synthetic and exception-backed.
- Run all applicable checks, create/verify `v0.1.0`, and push it over SSH.

## Non-Goals

- No claim that synthetic traffic is production traffic.
- No exception beyond v0.1.0 or beyond the specified real-production pcap gate.
- No raw pcap, local evidence, private path, credential, or unknown remote.

## Files to Inspect

- `scripts/release_gate.py`
- `docs/evidence/release-evidence-template.md`
- `docs/release-readiness.md`
- `Makefile`
- `.github/workflows/release.yml`
- `.github/workflows/docker-publish.yml`

## Files to Modify

- `scripts/release_gate.py`
- `docs/audit/release_exception_v0.1.0.yaml`
- `docs/evidence/release-v0.1.0.md`
- `docs/release-readiness.md`
- `README.md`
- `docs/development.md`
- this plan and task state

## Docs to Update

- Document the exception scope, expiry, evidence boundary, and release result.

## Dependency Impact

None. YAML is parsed with a constrained standard-library key/value reader; no dependency is added.

## Performance Impact

Negligible. The gate reads two small text files before release work.

## Quality Gates

- `make release-gate`
- `make shell-check`
- `make python-check`
- `make docs-check`
- `make deps-check`
- `make e2e-smoke`
- `git diff --check` and secret/path review
- tag object and remote SHA verification

## Execution Steps

1. Persist this plan and approval metadata.
2. Implement strict exception parsing and gate behavior.
3. Write the reviewed evidence record with synthetic-only and exception-backed wording.
4. Run all local checks and review staged contents.
5. Commit and push the release-gate/evidence change.
6. Create an annotated signed tag if a signing key is available; otherwise stop before tag creation.
7. Push the tag and record workflow verification status.

## Validation Plan

The gate passes only when the exception has the exact version, scope, approver, timestamp, revocation condition, and evidence note; fuzz evidence remains complete; synthetic pcap evidence is present; all sensitive-information fields are no; and the public record explicitly states the exception.

## Risk Points

- Exception metadata could be too broad or missing expiry.
- Public evidence could accidentally imply production traffic.
- Tag publication could occur before local gate success.

## Rollback Plan

If validation fails before tag creation, revert the release-evidence and gate commit. If a tag is already pushed, stop and follow owner-directed release withdrawal; do not overwrite the tag.

## Acceptance Criteria

- `make release-gate` passes with the approved v0.1.0 exception.
- All other local checks pass.
- `v0.1.0` is created only from the validated commit and points to it.
- Tag push uses the approved SSH remote.
- Remaining v0.1.1 real-pcap work is documented.

## Current Execution Record

- `make release-gate`: passed with the valid v0.1.0 scoped exception.
- `SKIP_DOCKER=1 make rc-check`: passed; coverage 74.2%, tests/race/ASan/e2e/dist checks passed.
- `make shell-check`, `make python-check`, `make docs-check`, `make deps-check`, and `make e2e-smoke`: passed.
- GPG secret keys are absent, but the existing approved SSH key successfully created and verified the signed annotated tag.

## Completion Record Before Tag

- Exception audit and public evidence committed and pushed in `3aacf9b release: 记录 v0.1.0 范围豁免与证据门禁`.
- `origin/main` is synchronized with the validated commit.
- The SSH-signed tag was created only after the local gate/RC checks passed.

## Tag and Workflow Record

- SSH-signed annotated `v0.1.0` created and verified against commit `32aafafc8b0d7cadbc53499a1b70d68a5e07ccc1`.
- Tag pushed successfully to the approved SSH remote.
- Public GitHub Release page shows v0.1.0 and two assets.
- GHCR image publication is not independently verifiable: anonymous manifest lookup returned denied and the public repository package view reports zero packages.
- GitHub Actions detail pages are not fully inspectable without authenticated `gh`; no GHCR success claim is made.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Success | Converted the approved exception request into a bounded, version-scoped release objective. | None |
