# Task Plan: R90-05 v0.1.1 release readiness

## Metadata

- Timestamp: 2026-07-16T15:34:38Z
- Branch: main
- Risk Level: High
- Remote baseline: `c753c18bd8e4df5b86c014d3c003a22264b86fc8`

## Goal

Prepare v0.1.1 release readiness from reviewed production-derived evidence and
make the release gate validate that evidence without relying on an exception.

## Authorized Deviation

The user explicitly waived the scheduled 2026-09-12 start date and authorized
R90-05 to begin on 2026-07-16. This authorization changes only the start date.

The user subsequently approved
`docs/audit/release_exception_r9005.yaml`, allowing only the exact synthetic
corpus digest and 7,500-packet count recorded there to substitute for
production-derived PCAP evidence in R90-05. The corpus must remain labeled
synthetic and the exception expires before R90-06.

## Scope

- Add and directly test a no-exception release-gate path for
  production-derived PCAP evidence.
- Run the current release-candidate and pinned supply-chain checks.
- Review approved local production-derived corpus evidence if it exists in the
  authorized workspace.
- Create a sanitized public v0.1.1 evidence record only from directly verified,
  reviewed results.
- Reconcile the roadmap, public release-readiness documentation, and task state.

## Non-Goals

- Do not reuse the v0.1.0 or R90-04 exceptions.
- Do not acquire, disclose, or fabricate private production traffic.
- Do not treat public, synthetic, generated, or fixture PCAPs as
  production-derived.
- Do not approve a release, begin R90-06, create or move a tag, publish a
  release, or publish an image.
- Do not request or handle an interactive sudo password.

## Risks

- A default exception argument can accidentally preserve the v0.1.0 bypass for
  v0.1.1.
- Public or synthetic traffic can be mislabeled as production-derived evidence.
- Final Docker RC validation depends on the approved execution environment
  remaining available for the corpus-backed acceptance run.
- Passing code-quality checks can be mistaken for release approval.

## Validation

- Direct release-gate tests for exception-free production evidence and rejection
  of false production claims.
- Historical v0.1.0 gate and expired R90-04 rejection regressions.
- `make python-check`, `make docs-check`, `make knowledge-check`, and
  `git diff --check`.
- `SUPPLY_CHAIN_FETCH_ASSETS=1 make supply-chain-check`.
- `VERSION=0.1.1 make rc-check`, including Docker when privilege is available.
- `make release-gate` against either reviewed production-derived evidence with
  no exception or the exact R90-05 synthetic corpus under its approved scoped
  exception.

## Acceptance Criteria

- v0.1.1 production-derived evidence can pass without an exception, and the
  R90-05 exception can pass only for its exact synthetic corpus while both
  historical exception boundaries remain enforced.
- The RC, supply-chain, and v0.1.1 release gates pass.
- Public documentation and evidence identify no unresolved release blocker and
  contain no private corpus path or sensitive data.
- The focused commit is pushed, fetched as `origin/main`, and synchronized to
  the local Vault using the exact pushed range.

## Stop Conditions

Stop without commit or release-readiness claims if completion requires a new
policy exception beyond the approved R90-05 record, an unverified corpus
change, final release approval, tagging, publication, or an ambiguous gate
result.

## Checkpoint Results

- Focused release-gate regressions passed: 13 tests cover exception-free
  production evidence, every promised substitute-evidence rejection, the
  expired R90-04 boundary, and the historical v0.1.0 fixture.
- `SUPPLY_CHAIN_FETCH_ASSETS=1 make supply-chain-check` passed with the pinned
  Go 1.25.12 toolchain, 9/9 locked external assets, and zero reachable
  vulnerabilities.
- `VERSION=0.1.1 SKIP_DOCKER=1 make rc-check` passed with 75.4% Go coverage,
  parser fuzz smoke, e2e smoke, and v0.1.1 distribution checks.
- `make python-check`, `make docs-check`, `make knowledge-check`, and
  `git diff --check` passed.
- Full `VERSION=0.1.1 make rc-check` passed, including Docker build,
  image-content smoke, and runtime health smoke.
- The exception-free release gate correctly stops because
  `docs/evidence/release-v0.1.1.md` does not exist. No approved
  production-derived corpus or local corpus-pressure evidence was found in the
  authorized workspace.
- Added `make pcap-evidence` and `make pcap-evidence-check`, a versioned JSON
  Schema, and a path-redacted generator/validator for classic pcap and pcapng.
  Automation computes inventory facts but requires human-supplied review
  decisions and fails on placeholders.
- The exception-free release gate now requires an approved manifest and exact
  local corpus. It reparses files and verifies the file set, packet counts, byte
  sizes, and SHA-256 values before accepting production-derived evidence.
- The earlier private production-corpus handoff dependency was superseded by
  the user's exact, R90-05-only synthetic-corpus exception.
- Timeline alignment requests a responsible owner and committed delivery date
  by 2026-07-20, approved corpus handoff by 2026-09-25, validation within one
  business day, and final release-gate acceptance by 2026-10-02. A missed
  checkpoint must be recorded as schedule impact to R90-05 and R90-06.
- The user approved the exact synthetic corpus as an R90-05-only policy
  exception. Its SHA-256 is
  `509e940bc275d1972c09a4d9fd061e942516e22a0931d44eb9eb24deb7c66e68`;
  its manifest, 711,108-byte size, and 7,500-packet count were revalidated.
- Corpus pressure passed for 1 pcapng and 7,500 processed packets with zero
  parse errors, drops, UDS write errors, or engine error-log lines.
- The public v0.1.1 evidence record is complete except for the distinct final
  release decision. The exception-backed gate verifies the corpus and stops
  only because `Metadata/Final decision` remains `pending` and
  `Approved for release` remains `no`.
- The user explicitly approved final v0.1.1 release-gate acceptance while
  retaining a separate tag/publication gate.
- Delivery commit `6c3f9ef276c99c13aa9e985b8c849bb5f0791752`
  is pushed and verified on fetched `origin/main`; post-fetch knowledge
  validation and the exact Vault range are verified.
- R90-05 is complete. Its exception is expired and cannot be reused for R90-06
  or another release gate.
