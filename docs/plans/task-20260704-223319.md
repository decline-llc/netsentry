# Task Plan: baseline-readiness-and-delivery-check

## Metadata

- Timestamp: 20260704-223319
- Branch: main
- Risk Level: Medium
- Required Skill: define-goal, security-best-practices
- Inherited Policies: DEP-03, DEP-04, DEP-05, DEP-06, DEP-07, DEP-08, DEP-09, DEP-11, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18

## Background

The user requested orderly progress toward NetSentry project delivery under the repository governance prompt. The repository already contains C capture, Go engine, SQLite storage, UDS communication, and API smoke-test paths. The first safe phase is to establish a current local baseline and identify delivery blockers without changing core protocol, database, pcap parsing, UDS, SQLite schema, or API response contracts.

## Goal

Restore the governance execution baseline, run existing project validation through native Makefile targets, and record any blockers or follow-up work needed for v0.1.0 delivery.

## Scope

- Verify repository state and remote configuration.
- Generate this task plan.
- Run existing validation and quality gates available through the Makefile.
- Perform passive Go security-best-practices checks while inspecting validation failures.
- Preserve existing behavior and avoid core-module implementation changes unless separately confirmed.

## Non-Goals

- No API response shape changes.
- No SQLite schema changes.
- No UDS protocol changes.
- No C packet parser behavior changes.
- No new v0.1.0-out-of-scope features such as IPv6 deep support, TLS decryption, TCP stream reassembly, distributed deployment, or frontend/auth system expansion.

## Files to Inspect

- Makefile
- README.md
- docs/api-reference.md
- docs/development.md
- docs/performance.md
- engine/go.mod
- engine/cmd/netsentry/main.go
- engine/internal/api/router.go
- capture/Makefile
- scripts/e2e_smoke.sh
- scripts/rc_check.sh

## Files to Modify

- docs/plans/task-20260704-223319.md

## Docs to Update

No product documentation changes are expected unless validation reveals stale build, test, API, deployment, dependency, or operational instructions. This planning document is required by governance policy.

## Dependency Impact

No dependency changes are planned. If Go dependencies are touched unexpectedly, run `go mod tidy` and `go mod verify`.

## Performance Impact

No runtime performance changes are planned. Existing `make bench` is available but may be treated as optional baseline evidence unless code changes touch performance-sensitive paths.

## Quality Gates

- `make e2e-smoke`
- `make test`
- `make lint`
- `make fuzz-parser` if C parser changes become necessary
- `make bench` if performance-sensitive changes become necessary
- Existing dependency or vulnerability checks if discovered in project tooling

## Execution Steps

1. Confirm repository state, branch, and remote.
2. Confirm native Makefile validation targets.
3. Run `make e2e-smoke` as the required baseline smoke test.
4. Run `make test` and `make lint` if smoke test passes or after fixing blockers.
5. Inspect failures without weakening tests or bypassing Makefile targets.
6. Apply only minimal, in-scope fixes after core-module gate review if needed.
7. Re-run failed validation after each substantive fix attempt.
8. Perform secret scan before staging, commit, or push.
9. Commit and push only verified, atomic task changes.

## Validation Plan

Primary validation is `make e2e-smoke`. Supporting validation includes `make test`, `make lint`, and command-specific dependency checks if files change. If Docker is used later, invoke it through `sudo docker` or `DOCKER="sudo docker"` as required.

## Risk Points

- The repository includes core modules where unconfirmed changes are not allowed.
- The working tree contains an untracked `.codex/` directory that must not be accidentally staged.
- Network access may be restricted for dependency downloads or pushing to the remote.
- `make lint` may skip `staticcheck` if it is not installed; this should be recorded rather than replaced with unapproved tooling.

## Rollback Plan

For this planning-only step, rollback is removal of the new planning document. If later code changes are made and validation fails after three repair attempts, follow the repository failure-handling policy and stop with an error log.

## Acceptance Criteria

- Repository remote matches the expected NetSentry remote.
- Required planning document exists.
- Required baseline validation results are recorded.
- No unrelated or sensitive files are staged.
- Any core-module changes are deferred until explicit confirmation.

## Execution Results

- Repository remote was corrected to `https://github.com/decline-llc/netsentry`.
- `make e2e-smoke` passed with 6 packets processed, 5 alerts generated, and 8 rules loaded.
- `make test` passed after running outside the sandbox because UDS tests require Unix socket bind permissions.
- `make lint` passed `go vet`; `staticcheck` was not installed and was skipped by the existing Makefile target.
- `go mod verify` passed with all modules verified.
- `make rc-check` passed after one retry for a transient Docker build `go mod download` network reset against `goproxy.cn`.
- RC coverage included shell syntax checks, `make test`, `make fuzz-parser`, `make e2e-smoke`, `make dist`, dist archive verification, Docker image smoke, and Docker runtime `/api/health` smoke.
- Docker operations were executed through a temporary `/tmp` wrapper that invoked `sudo -S docker`; the wrapper was deleted after validation and no password was written to the repository.
- Secret scan found only documented token placeholders and redaction test fixtures, not real credentials.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Applied | Task narrowed to baseline readiness and delivery check with measurable validation commands. | Not needed |
| security-best-practices | Applied | Go backend security reference loaded for passive secure-by-default checks; C checks covered by project governance rules. | Not needed |
