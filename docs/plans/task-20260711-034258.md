# Task Plan: release evidence prerequisite audit

## Metadata

- Timestamp: 2026-07-10T20:42:58-07:00
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: DEP-03, DEP-04, DEP-05, DEP-06, DEP-08, DEP-10, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18, DEP-19

## Background

The authoritative v0.1.0 plan identifies sustained external C fuzz evidence and realistic sanitized pcap corpus pressure/query evidence as remaining pre-tag gates. The workspace contains generated synthetic corpus files under `/tmp`, but no operator-supplied external corpus is present in the repository. Synthetic inputs must remain clearly separated from release evidence.

## Goal

Verify the current release-evidence prerequisites, preserve the distinction between synthetic regression evidence and operator-supplied evidence, and leave an auditable next action without creating the `v0.1.0` tag.

## Scope

- Repository, branch, remote, tag, Makefile target, and corpus inventory checks.
- Record the synthetic corpus classification and remaining release blockers.
- Run documentation, dependency, whitespace, and existing smoke validation.

## Non-Goals

- No C/Go/runtime/API/SQLite/schema/config/CI changes.
- No fabrication or publication of external fuzz or realistic-traffic evidence.
- No raw corpus or local evidence files committed.
- No version tag or release publication.

## Files to Inspect

- `.codex/system_prompt.md`
- `First/NETSENTRY_MASTER_PLAN.md`
- `docs/release-readiness.md`
- `docs/tasks/task-state-20260708-051113.json`
- `docs/tasks/task-state-20260711-030316.json`
- `Makefile`

## Files to Modify

- This plan.
- `docs/tasks/task-state-20260711-034258.json`.

## Docs to Update

- Persisted task state only; public release-readiness wording already states the correct blocker status.

## Dependency Impact

None. No dependency files or build definitions change.

## Performance Impact

None. This audit does not run a long pressure campaign.

## Quality Gates

- `make docs-check`
- `make deps-check`
- `make e2e-smoke`
- `git diff --check`
- staged-diff secret/path review

## Execution Steps

1. Confirm repository, branch, approved SSH remote, and absence of the release tag.
2. Inventory workspace and temporary corpus candidates without treating generated samples as external evidence.
3. Persist the verified blocker and next operator action.
4. Run applicable quality gates and review the final diff.

## Validation Plan

The audit passes when the repository remains clean apart from this plan/state pair, all applicable Makefile checks pass, no dependency files change, no secret/private path is staged, and the task state preserves the three pre-tag blockers.

## Risk Points

- Synthetic corpus could be misreported as realistic external traffic evidence.
- Local temporary paths could leak into committed documentation.
- An unapproved tag or release action could prematurely close the release process.

## Rollback Plan

Revert only this documentation/state pair if the release owner changes the evidence policy. Do not alter runtime code or generated local evidence.

## Acceptance Criteria

- Approved SSH origin remains unchanged.
- `v0.1.0` tag is absent.
- Synthetic `/tmp` corpus is explicitly classified as non-release evidence.
- External fuzz, realistic corpus pressure/query, and tag-driven publication remain blocked.
- Applicable quality gates pass.

## Completion Record

- `make docs-check`: passed.
- `make deps-check`: passed; all Go modules verified.
- `make e2e-smoke`: passed with 6 packets, 5 alerts, and 8 rules loaded.
- `git diff --check`: passed.
- Secret/path review: passed; no credentials, private keys, or local absolute paths staged.
- Commit: `8e60e55 docs: 固化 v0.1.0 发布门禁前置审计`.
- Push: succeeded to `origin/main` over the approved SSH remote.
- `v0.1.0` tag: not created.
- GitHub PR: not created because `gh auth status` has no authenticated host session.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Success | Converted the broad rollout request into a bounded, evidence-based release prerequisite audit. | None |
| yeet | Partial | GitHub CLI is installed but has no authenticated host session; SSH Git push remains the project-required delivery path. | Do not create a PR in this session; retain the atomic commit/push record and request `gh auth login` before any PR workflow. |

## Skill Fallback Record

| Skill | Failure Type | Time | Fallback Template | Impact | Final Decision |
|---|---|---|---|---|---|
| yeet | GitHub CLI unauthenticated | 2026-07-10T20:43:00-07:00 | Manual Git Add -> Commit -> SSH Push, without PR creation | PR creation deferred; repository commit/push can still be audited | Continue with required SSH delivery; report PR limitation |
