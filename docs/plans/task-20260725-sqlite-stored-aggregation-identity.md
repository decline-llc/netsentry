# Task Plan: R90-33 validate stored SQLite aggregation identity

## Metadata

- Timestamp: 2026-07-25T09:17:15-07:00
- Branch: main
- Risk Level: Medium
- Remote baseline: `d6ff41dff8599cff718fbe627ce64c9ee85b2053`

## Goal

Reject persisted alert rows whose public `id` disagrees with the canonical
aggregation identity generated by the writer.

## Scope

- Derive the alert ID through one shared helper used by normalization and row
  decoding.
- Reject empty or altered stored IDs that do not match the aggregation tuple.
- Cover primary list/query decoding and historical cross-shard reads.
- Prove a rejected historical shard remains byte-for-byte unchanged.
- Reconcile storage documentation, roadmap status, and task state.

## Non-Goals

- Do not change the aggregation-ID format or aggregation tuple.
- Do not validate `event_id` against the event ledger or change event
  deduplication.
- Do not repair, delete, migrate, or rewrite invalid rows.
- Do not create a release tag or publish artifacts.

## Risks

- A duplicated ID derivation could drift between the writer and reader.
- Rejecting IDs outside the writer contract can expose previously hidden
  operator-modified data.
- Historical rejection must stay on the existing read-only query path.

## Validation

- Direct empty and altered ID rejection regressions across list and query
  decoding.
- Direct historical-shard rejection with byte preservation and an encoded
  filesystem path.
- Existing healthy aggregation, pagination, and cross-shard compatibility.
- Twenty uncached focused alert-store race runs.
- Complete native test suite, E2E smoke, documentation, knowledge, JSON, diff,
  and sensitive-information checks.

## Acceptance Criteria

- Every decoded row ID equals the canonical identity derived from `rule_id`,
  `src_ip`, `dst_ip`, `dst_port`, and `window_start`.
- Writer normalization and row decoding use the same ID derivation.
- Valid rows retain current aggregation, list, query, pagination, and
  cross-shard behavior.
- A rejected historical shard remains byte-for-byte unchanged.
- Required validation passes and delivery is verified against fetched
  `origin/main` and the local Vault.

## Stop Conditions

Stop if completion requires changing the aggregation identity, automatic row
repair, deletion, schema migration, event-ledger reconciliation, operator data,
tag creation, or publication authority.

## Delivery Evidence

- Feature commit: `824de0ee51fa5841d17021e797ba1f293c7aa128`
- Twenty uncached focused alert-store race runs passed.
- Complete native race suite, E2E smoke, documentation, knowledge, JSON, diff,
  and sensitive-information checks passed.
- Fetched `origin/main`: verified equal to the feature commit after push.
- Post-fetch knowledge check: passed.
- Vault range: `d6ff41dff8599cff718fbe627ce64c9ee85b2053..824de0ee51fa5841d17021e797ba1f293c7aa128`
- Vault iteration note, full commit index, and `00-MOC` link: verified.
