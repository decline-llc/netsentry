# R90-113: Production SLO acceptance contract

## Selection and scope

The user selected production SLO evaluation and formally added live-arrival to
durable-persistence latency, offered-versus-completed packet accounting,
missing-alert failure accounting, raw counts/deadline violations beside p99,
and extended-duration tail validation. The initial runner description was
bench01, separate from the NetSentry instance; before delivery the user
clarified that no external host exists and authorized isolated same-VM testing.
The final contract uses that clarified scope; no qualifying evidence exists.
Record this material authority change as one complete documentation increment.
R90-75 remains incomplete until execution and acceptance evidence exist.

Baseline: `55019110e3227028236cd2478623525b3f77d939`, clean and freshly
fetched equal to HEAD, origin/main and FETCH_HEAD. The R90-112 feature/closure
parent chains, exact Vault notes, index rows and MOC links are verified.
All 128 prior states parse; 116 unique roadmap rows/Definitions match. The
Aug 26-Sep 23 phase contains ten documentation commits, with no intervening
runtime change. R90-59 retains its independent release blocker; R90-75's
missing-product-choice and unnamed-runner statements are now superseded.

## Intended paths

- `docs/performance-slo.md`: target profiles, normative measurement contract,
  artifact destination/content, source-grounded gaps and execution prerequisites.
- `docs/performance.md`: link the contract and replace stale scope/runner claims.
- `docs/plans/rolling-90-day-roadmap.md`: reconcile R90-75 and add R90-113.
- This plan and `docs/tasks/task-state-20260923-production-slo-contract.json`.
- `docs/tasks/task-state-20260923-production-slo-acceptance.json`: active R90-75
  evidence status and remaining prerequisites, without a compliance claim.
- Reconcile affected local stable Vault prose after verified delivery.

## Non-goals and authority

No runtime, benchmark, dependency, toolchain, configuration, threshold-gate,
traffic-generation, candidate, tag, publication or external mutation. Do not
fabricate a completed acceptance artifact or treat a contract as capacity
proof. Do not silently change the approved local execution scope or normalize
the designated artifact directory's Unicode hyphen. No private traffic or credentials are
required to deliver this documentation increment.

## Acceptance and evidence map

| Acceptance | Direct evidence |
| --- | --- |
| Exact product direction and proposed targets retained | User-approved contract clauses; staging/production table; bandwidth/mean-packet arithmetic; no demonstrated capacity claim |
| End-to-end and loss semantics cannot be substituted | Source comparison with capture loop, worker matching/write timers and pre-processing counter; explicit missing-alert accounting |
| Tail evidence remains reviewable | Raw counts, violations and p99 together; extended run requirement; pending duration/sample policy |
| Clarified local context and artifact location retained | User correction supersedes external bench01 prerequisite; separate directories/process groups/fresh service state; exact destination; no synthetic result file |
| Outstanding validation is explicit | Historical SSH preflight superseded; local resource discovery and production RAM shortfall; absent qualifying runs and measurement coverage |
| Durable queue and knowledge are accurate | R90-75 remains blocked on evidence/resources/measurement, with product direction supplied; R90-113 is one documentation increment |

## Validation and stop conditions

Run benchmark-baseline-check, docs-check, knowledge-check, all task-state JSON
parsing, complete row/Definition multiset and chronology checks, profile rate
arithmetic, exact six-path diff review and anchored sensitive-data review.
Validate full-SHA Git/remote/Vault ranges after push and replay to verify stable
prose survives synchronization. Use one docs-only closure for delivery facts.
Stop before execution without isolated local setup, matching profile resources,
frozen workload/configuration and measurement implementation; stop delivery
on ambiguous local validation or remote/Vault evidence.

## Preflight deviation

Read-only SSH to bench01 failed before authentication: hostname resolution
reported a temporary failure. No remote commands or traffic ran. The user has
been asked for a reachable address/alias and the SUT/ingress assignment.
This was an initial execution blocker. The subsequent user clarification below
supersedes it; no external runner or SSH access is required.

## User correction before delivery

The user explicitly permits completion of the acceptance contract and clarifies
all runs will execute inside this Ubuntu VM with separate working directories,
isolated process groups and no production runtime reuse. This replaces the
independent-environment prerequisite. It authorizes local execution scope, not
a claim of independent hardware. Preserve the original artifact template.
Read-only local discovery reports 16 logical CPUs and 8,078,816 KiB guest RAM,
with lo/ens33/ens37/docker0 interfaces. No ingress was selected and no traffic
ran. The 16 GiB production profile cannot qualify on this memory allocation;
actual profile/resource agreement remains required. Documentation scope stays
at the same six paths; R90-75 remains incomplete and no benchmark run starts.

## Sep 25 continuation and validation checkpoint

After the user's continue instruction, a fresh fetch again verified HEAD,
origin/main and FETCH_HEAD at the unchanged R90-112 closure. Only the six
planned in-progress paths are present. Read-only local resources are unchanged.
The active rolling horizon advances to Sep 25-Dec 23 (90 days) as routine
roadmap reconciliation; the original contract date and filenames are retained.

All 130 task states parse and all 117 roadmap rows/Definitions match as complete
multisets without duplicates. Contract review verifies all three formal clauses,
local-only execution with no SSH prerequisite, unchanged proposed targets,
packet-rate arithmetic, exact artifact-template identity, production RAM gap,
and absent qualifying evidence. Local links, chronology, six-path scope,
formatting and sensitive-data checks pass. The R90-74 baseline revalidation,
docs check and all 33 knowledge tests pass; final pre-commit checks include the
refreshed horizon. Documentation validation does not validate either SLO profile.
No direct runtime regression is promised or executed by this increment.
The existing skill's authority and evidence rules cover the user correction;
no reusable skill change is needed. Delivery and exact-range Vault verification
remain before R90-113 completion.
