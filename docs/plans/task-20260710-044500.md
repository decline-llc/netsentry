# Task Plan: restore SSH Git push standard

## Metadata

- Timestamp: 20260710-044500
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: ENV-01, ENV-03, DOC-01, DOC-02, QUALITY-03, DEPS-01, GIT-04

## Goal

Restore both `origin` URLs to `git@github.com:decline-llc/netsentry.git`, make
SSH the explicit and only project push standard in `.codex/system_prompt.md`,
correct the latest release state, remove the superseded HTTPS push failure log,
and create one local documentation commit without pushing it.

## Scope

- `.codex/system_prompt.md`
- `docs/tasks/task-state-20260708-051113.json`
- `docs/release-readiness.md`
- Removal of `docs/errors/git-push-fail-20260710-043041.log`
- This task plan

## Non-Goals

- Do not modify business code, tests, build scripts, dependencies, or history.
- Do not stage `docs/evidence/local/`.
- Do not push the resulting commit.

## Validation Plan

- Verify both remote URLs are the SSH standard.
- Run `make docs-check` and `make deps-check`.
- Validate every task-state JSON file with Python's JSON parser.
- Run `git diff --check` and inspect the staged file list.

## Acceptance Criteria

- The core governance prompt contains no HTTPS push requirement and explicitly
  prohibits automatic HTTPS switching.
- `.codex/system_prompt.md` is local/ignored and contains an existing system
  password, so its verified local correction is intentionally not staged or
  committed to prevent sensitive-information exposure.
- Latest state says HTTPS was superseded and SSH is the standard.
- The specified HTTPS push failure log is removed.
- Only documentation/configuration and state files are in the atomic commit.
- No push is attempted.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Defined measurable SSH restoration and validation outcomes. | None |
