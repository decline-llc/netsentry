# Task Plan: rebuild rolling roadmap capability

## Metadata

- Timestamp: 2026-07-14T07:28:02Z
- Branch: main
- Risk Level: Low

## Goal

Establish the repository-backed 90-day roadmap that `$netsentry-next` uses to choose and finish one dependency-ready delivery increment per trigger.

## Scope

- Add the initial dated roadmap, dependencies, acceptance criteria, and blocked evidence boundary.
- Deploy the `netsentry-roadmap` capability and connect `$netsentry-next` to its one-increment workflow.

## Non-Goals

- Do not modify NetSentry product behavior or begin R90-02.
- Do not represent the agent as an unattended scheduler.
- Do not acquire or fabricate production-derived traffic evidence.

## Validation and Stop Conditions

- Validate both skill Markdown files and the generated skill metadata.
- Run `make docs-check` and `git diff --check` for repository documentation.
- Stop if a requested scheduled increment needs new authority or external/private evidence.
