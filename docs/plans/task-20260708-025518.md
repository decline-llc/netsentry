# Task Plan: v0.1.0 release readiness evidence index

## Metadata

- Timestamp: 20260708-025518
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: ENV-01, ENV-02, ENV-03, SKILL-01, SKILL-02, DOC-01, DOC-02, QUALITY-03, DEPS-01, VAL-08, GIT-01

## Background

The v0.1.0 release readiness status is documented across README, the development guide, changelog, and persistent development notes. The remaining blockers are evidence-driven rather than implementation-heavy: sustained external C fuzz evidence, realistic pcap corpus pressure/query evidence, and the final tag/release/publish flow. A public checklist should make the release gate state easier to verify without exposing private corpus paths or local evidence files.

Repository check found that `origin` currently points to `git@github.com:decline-llc/netsentry.git`, while the local governance prompt requires `https://github.com/decline-llc/netsentry`. Local implementation and validation can continue, but push must be skipped unless the remote is corrected or explicitly approved.

## Goal

Add a public v0.1.0 release-readiness evidence index that lists wired gates, latest local evidence, remaining blockers, exact evidence commands, and commit-safety rules for local-only evidence.

## Scope

- Add `docs/release-readiness.md`.
- Link it from `README.md`.
- Reference it from `docs/development.md`.
- Record the addition in `CHANGELOG.md`.
- Keep this as documentation only.

## Non-Goals

- Do not tag v0.1.0.
- Do not publish a GitHub Release or registry image.
- Do not run private external corpora.
- Do not change code, API responses, SQLite schema, UDS protocol, parser behavior, or dependencies.

## Files to Inspect

- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `.gitignore`
- `Makefile`

## Files to Modify

- `docs/release-readiness.md`
- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `docs/plans/task-20260708-025518.md`

## Docs to Update

- New release-readiness evidence index.
- README release-readiness section.
- Development guide release-candidate section.
- Changelog unreleased entry.

## Dependency Impact

No dependency changes expected. Run `make deps-check` to verify the existing Go module cache integrity.

## Performance Impact

No runtime or benchmark behavior changes.

## Quality Gates

- `make docs-check`
- `make shell-check`
- `make python-check`
- `make config-check`
- `make deps-check`
- `make lint`
- `git diff --check`

## Execution Steps

1. Add the release-readiness evidence index.
2. Link the index from README and development docs.
3. Update changelog.
4. Run documentation, syntax, config, dependency, and lint gates.
5. Scan the diff for secrets and local evidence paths.
6. Commit locally if all gates pass.
7. Skip push until the remote URL policy mismatch is resolved.

## Validation Plan

This is a documentation-only change, so full `make e2e-smoke` is not required by the changed surface. The planned gates verify public docs, scripts, config parsing, dependencies, and Go static analysis. No Docker commands are expected.

## Risk Points

- The new document must not imply that v0.1.0 release blockers are closed.
- The document must not include private corpus paths, local evidence contents, credentials, or sudo password material.
- Push is blocked by the remote URL policy mismatch unless resolved.

## Rollback Plan

Revert the new release-readiness document and the three public doc references.

## Acceptance Criteria

- Public docs expose one canonical v0.1.0 release-readiness evidence index.
- Remaining blockers remain explicit.
- Local-only evidence boundaries are clear.
- Quality gates pass.
- No secret material appears in the diff.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Goal narrowed to a documentation-only release-readiness evidence index with clear remaining blockers and validation gates. | None |
