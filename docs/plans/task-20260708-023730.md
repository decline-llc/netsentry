# Task Plan: full sudo Docker release-candidate validation

## Metadata

- Timestamp: 20260708-023730
- Branch: main
- Risk Level: Medium
- Required Skill: define-goal
- Inherited Policies: planning-first, sudo-docker, validate-before-commit, docs-sync, dependency-check

## Background

The v0.1.0 release readiness checklist still requires a final full `sudo docker` release-candidate check. The previous task added the realistic corpus evidence entrypoint, so the current tree needs a fresh complete RC validation with Docker enabled.

## Goal

Record that the current release-candidate tree passes the full native `make rc-check` bundle with Docker operations executed through sudo, then commit and push that validation evidence.

## Scope

- Add a task plan for the RC validation run.
- Update public release-readiness docs with the latest local full sudo Docker RC evidence.
- Run the full RC bundle on the final edited tree.
- Update local-only persistent development records.

## Non-Goals

- Do not tag v0.1.0.
- Do not publish a GitHub Release or registry image.
- Do not change C/Go runtime behavior.
- Do not add dependencies.

## Files to Inspect

- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `scripts/rc_check.sh`
- `Makefile`

## Files to Modify

- `docs/plans/task-20260708-023730.md`
- `README.md`
- `docs/development.md`
- `CHANGELOG.md`

## Docs to Update

- README release-readiness checklist.
- Development guide release-readiness checklist.
- Changelog unreleased release-candidate evidence.

## Dependency Impact

No dependency changes expected. `make deps-check` remains part of the RC bundle and will be run independently as a post-check.

## Performance Impact

No code performance impact. The RC bundle runs existing tests, fuzz smoke, e2e smoke, dist packaging, Docker image build, and Docker runtime health smoke.

## Quality Gates

- `DOCKER="sudo docker" make rc-check` or an equivalent non-interactive sudo Docker invocation.
- `make docs-check`
- `make deps-check`
- `make lint`
- `git diff --check`

## Execution Steps

1. Update release-readiness docs and this plan.
2. Run full RC validation with Docker enabled through sudo.
3. Run supplemental lightweight quality gates if not already covered or to capture concise post-check evidence.
4. Scan for accidental secrets or local evidence artifacts.
5. Commit and push a single validation evidence commit.

## Validation Plan

The main validator is a complete `make rc-check` run with Docker enabled. The run must cover shell/docs/python/config/deps checks, C/Go tests, coverage, deterministic parser fuzz smoke, e2e smoke, dist archive smoke, Docker build, Docker image content smoke, and Docker runtime `/api/health` smoke.

## Risk Points

- Non-interactive sudo Docker may fail if credential caching is unavailable.
- Docker build may fail on transient registry or apt mirror issues.
- The RC run is long and may expose unrelated flakiness.

## Rollback Plan

If validation cannot pass after bounded fixes, revert the documentation changes for this task and write a failure log under `docs/errors/`.

## Acceptance Criteria

- Full sudo Docker RC bundle passes on the final edited tree.
- Public docs record the validation without claiming v0.1.0 is tagged or released.
- No sensitive sudo helper, corpus evidence, or generated artifacts are committed.
- A single commit is pushed to `origin/main`.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Goal narrowed to recording full sudo Docker RC evidence for the current release-candidate tree. | None |
