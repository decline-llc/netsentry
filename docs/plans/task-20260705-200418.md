# Task Plan: governance-baseline-remote-and-cache-validation

## Metadata

- Timestamp: 20260705-200418
- Branch: main
- Risk Level: Low
- Required Skill: define-goal, security-best-practices
- Inherited Policies: DEP-01, DEP-02, DEP-03, DEP-04, DEP-05, DEP-06, DEP-09, DEP-10, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18

## Background

The project governance prompt in `.codex/system_prompt.md` requires repository baseline checks before continuing delivery work. The current local remote is configured as `git@github.com:decline-llc/netsentry.git`, while the governance document expects `https://github.com/decline-llc/netsentry`. The previous task also changed Makefile Go cache handling so native Makefile targets work in environments where the home-directory Go build cache is read-only.

## Goal

Restore the governance baseline by checking the local remote URL, verify the Makefile cache fix through native validation targets, and record security/quality/dependency results without changing core NetSentry behavior.

## Scope

- Inspect repository state, branch, and remote.
- Check the local `origin` remote URL and reconcile it with project push requirements.
- Keep prior Makefile cache behavior and documentation updates intact.
- Run validation and dependency checks relevant to the changed build tooling and docs.
- Record results in this plan and the local master plan.

## Non-Goals

- No protocol changes.
- No SQLite schema changes.
- No API response shape changes.
- No pcap parser behavior changes.
- No new dependencies.
- No Docker execution unless a later validation command requires it; if used, Docker must be invoked through sudo.
- No commit or push without explicit user approval, because the local master plan requires reporting and approval before push.

## Files to Inspect

- `.codex/system_prompt.md`
- `First/NETSENTRY_MASTER_PLAN.md`
- `Makefile`
- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `docs/plans/task-20260704-223319.md`

## Files to Modify

- `docs/plans/task-20260705-200418.md`
- Local Git remote configuration
- `First/NETSENTRY_MASTER_PLAN.md`

## Docs to Update

- This task plan.
- Local master plan task log.

## Dependency Impact

No dependency files are expected to change. Run `go mod verify` to confirm existing Go module integrity.

## Performance Impact

No runtime performance impact is expected. The Makefile cache setting affects build/test cache location only.

## Quality Gates

- `make lint`
- `git diff --check`
- `make e2e-smoke`
- `go mod verify`

## Execution Steps

1. Read `.codex/system_prompt.md` and applicable skills.
2. Confirm repository state, branch, and remote.
3. Create this planning document.
4. Update `origin` to `https://github.com/decline-llc/netsentry`.
5. Re-run repository state checks.
6. Run security-oriented secret scan over changed files.
7. Run quality and dependency validation.
8. Update this plan and the local master plan with results.
9. Stop before commit/push and report for user approval.

## Validation Plan

Use the native Makefile validation path. `make e2e-smoke` may require elevated execution outside the sandbox because it creates local sockets. Docker is not planned for this task.

## Risk Points

- The working tree already contains prior uncommitted edits and a pre-existing `.gitignore` change.
- Updating `.git/config` may require execution outside the workspace write sandbox.
- The governance prompt asks for commit/push, but the local master plan requires reporting and waiting for user approval first. The safer local project rule wins for this run.

## Rollback Plan

If the remote URL update is incorrect, run `git remote set-url origin git@github.com:decline-llc/netsentry.git` or another user-approved URL. If validation fails due to the Makefile cache change, revert only the Makefile/docs cache changes from the prior task after preserving user-owned `.gitignore` edits.

## Acceptance Criteria

- `origin` remote is known and points to the expected NetSentry repository.
- Changed files contain no real secrets.
- `make lint`, `git diff --check`, `go mod verify`, and `make e2e-smoke` pass or have documented environment-specific escalation.
- No Docker command is run without sudo.
- No commit or push is performed without user approval.

## Execution Results

- Repository check passed. `origin` was briefly updated to `https://github.com/decline-llc/netsentry` based on the written governance prompt, then restored to `git@github.com:decline-llc/netsentry.git` after the user clarified that repository pushes should use SSH.
- `git diff --check` passed.
- Secret scan over changed files found only the documented `${NETSENTRY_API_TOKEN:}` placeholder in `docs/development.md`; no real credentials were found.
- `make lint` passed. `staticcheck` was not installed and was skipped by the existing Makefile target.
- `go mod verify` passed with all modules verified.
- Initial sandboxed `make e2e-smoke` failed during Python socket creation with `PermissionError: [Errno 1] Operation not permitted`, matching the known sandbox socket limitation.
- Elevated `make e2e-smoke` passed with 6 packets processed, 5 alerts generated, and 8 rules loaded.
- No Docker commands were executed.
- No commit or push was performed during this validation task; later commit/push work is tracked in `docs/plans/task-20260705-200707.md`.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Applied | Goal narrowed to remote baseline alignment plus validation of existing Makefile cache behavior. | Not needed |
| security-best-practices | Applied | Go backend security reference loaded for passive checks; no Go source or HTTP behavior changes planned. | Not needed |
