# Task Plan: add release readiness checklist

## Metadata

- Timestamp: 20260706-220953
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: native Makefile, v0.1.0 boundary, docs sync, dependency check, quality gate, sensitive-info scan

## Background

NetSentry has release-candidate checks, local archives, Docker image smoke tests, CI, GHCR publishing workflow, performance baselines, and public known gaps. The remaining v0.1.0 release blockers are still spread across README, development notes, performance notes, and the local development log. A concise release readiness checklist will make the next landing steps explicit without expanding the v0.1.0 scope.

## Goal

Add a public v0.1.0 release readiness checklist that separates already-ready release gates from remaining blockers, so future sessions can advance release prep in single, verifiable tasks.

## Scope

- Add a release readiness checklist to public docs.
- Keep remaining blockers aligned with current known gaps: sustained fuzz evidence, realistic pcap corpus pressure/query checks, final full `make rc-check` with sudo Docker, named GitHub release, and named registry image.
- Update changelog and ignored local development state.
- Record validation results in this plan.

## Non-Goals

- No release tag creation.
- No GitHub Release publishing.
- No Docker image publishing.
- No source code, API, config, or Dockerfile behavior changes.

## Files to Inspect

- `README.md`
- `docs/development.md`
- `docs/performance.md`
- `CHANGELOG.md`
- `DEVLOG.md`
- `scripts/rc_check.sh`

## Files to Modify

- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `DEVLOG.md` (ignored local state file)
- `docs/plans/task-20260706-220953.md`

## Docs to Update

- README release-candidate section.
- Development guide release readiness section.
- Changelog unreleased entry.
- This task plan.

## Dependency Impact

No dependency changes. Run `make deps-check` to verify current Go modules.

## Performance Impact

No runtime performance impact. This is documentation-only.

## Quality Gates

- `git diff --check`
- `make docs-check`
- `make shell-check`
- `make python-check`
- `make deps-check`
- `make lint`

## Execution Steps

1. Review current release, Docker, CI, performance, and known-gap documentation.
2. Add a concise release readiness checklist to README and development docs.
3. Update CHANGELOG and local `DEVLOG.md`.
4. Run validation and sensitive-info scan.
5. Commit and push tracked documentation changes.

## Validation Plan

Use documentation and repository quality gates because no runtime behavior changes are planned. Do not execute Docker because this task only documents release readiness; the checklist will explicitly require a later full sudo-Docker `make rc-check` before release.

## Risk Points

- Avoid claiming v0.1.0 is released.
- Do not list optional post-v0.1.0 enhancements as release blockers.
- Preserve the requirement that Docker validation must run through sudo when performed.

## Rollback Plan

Revert the README, development guide, changelog, local DEVLOG update, and this plan.

## Acceptance Criteria

- Public docs expose a clear v0.1.0 readiness checklist.
- Checklist distinguishes completed gates from remaining blockers.
- Docker release validation is documented as sudo-required.
- Validation gates pass.

## Validation Result

- `git diff --check`: passed.
- `make docs-check`: passed.
- `make shell-check`: passed.
- `make python-check`: passed.
- `make deps-check`: passed, `go mod verify` reported all modules verified.
- `make lint`: passed; `staticcheck` was not installed and was skipped by the Makefile.
- Sensitive-pattern scan over changed tracked docs plus local `DEVLOG.md` found only the existing documented `${NETSENTRY_API_TOKEN:}` placeholder in `docs/development.md`.
- Docker was not executed because this documentation-only task does not change Docker behavior; the new checklist explicitly requires final release validation through `DOCKER="sudo docker" make rc-check`.

## Security Check

No key, token, private key, sudo password, or production secret was added. The only scanner match is an existing environment-variable placeholder documented for API auth configuration.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Applied | Goal narrowed to a documentation-only release readiness checklist with explicit blockers and pass/fail validation commands. | Not needed |
