# Task Plan: R90-59a authorized local v0.1.1 tag

## Metadata

- Timestamp: 2026-08-07T01:57:16-07:00
- Branch: main
- Risk Level: High
- Remote baseline: `c19067172f1c626a59ba11b3201b276092721192`
- Candidate: `78cd78574e03c8f73ff68248eed2c409d6bca406`
- Tag: `v0.1.1`

## Goal

Create and cryptographically verify only a local annotated `v0.1.1` Git tag at
the exact R90-58 candidate after a fresh candidate changelog/release-evidence
review and smoke validation, while proving that no remote tag, GitHub Release,
GHCR image, or workflow dispatch is created.

## Scope

- Reconcile the user's exact tag-only authorization with R90-58 evidence.
- Review the candidate's `CHANGELOG.md`, release evidence, version, and commit.
- Validate the exact candidate from an isolated clean detached worktree.
- Create a signed annotated local tag `v0.1.1` pointing exactly to the candidate.
- Verify the tag object, signature, peeled commit, and continued absence of the
  remote tag and publication outputs.
- Record the partial R90-59 boundary in the roadmap and task state.

## Non-Goals

- Do not push `v0.1.1` or any other tag.
- Do not create a GitHub Release, publish a GHCR image, dispatch a workflow, or
  modify a workflow to bypass its current tag triggers.
- Do not move or recreate the candidate, change runtime/release behavior, or
  rewrite changelog content at the historical candidate.
- Do not claim R90-59 complete; remote publication remains separately blocked.
- Do not invent comparable-environment benchmark evidence or a product/SLO
  budget, and do not start R90-75.

## Risks

- Pushing the tag would automatically trigger both the GitHub Release and GHCR
  workflows, violating the current authorization boundary.
- An unsigned, lightweight, mistargeted, or unverifiable tag would not satisfy
  the immutable release-reference contract.
- The candidate changelog has no `0.1.1` heading; its content is still under
  `[Unreleased]`, so remote publication must remain blocked pending explicit
  changelog approval and later action authority.
- Running validation from current `main` instead of the historical candidate
  would not prove the tagged payload.

## Acceptance Criteria and Evidence Map

| Acceptance criterion | Planned evidence |
| --- | --- |
| Exact authority and identity | User authorization, candidate ancestry, absent local/remote tag, exact version/SHA comparison |
| Candidate review | Candidate `CHANGELOG.md` and `docs/evidence/release-v0.1.1.md` hashes plus direct content review |
| Candidate smoke passes | Clean detached candidate worktree; v0.1.1 RC/E2E/archive checks and release gate pass |
| Local tag is immutable and authentic | Annotated signed tag object, signature verification, and peeled target equal the exact candidate |
| No unauthorized external mutation | `git ls-remote` still reports no `v0.1.1`; no tag push, workflow dispatch, GitHub Release, or GHCR action |
| Repository evidence is recoverable | Roadmap/task-state/docs validation, fetched `origin/main`, exact Vault range, note/index/MOC/stable-release reconciliation |

## Trigger Audit

- Fetched `origin/main` and tags; clean `HEAD == origin/main` at
  `c19067172f1c626a59ba11b3201b276092721192`.
- Verified the R90-74 feature/closure Git and exact Vault note/index/MOC chain;
  all 89 prior task-state files parse and all 78 roadmap rows match one
  Definition.
- Reviewed 152 commits since Jul 14 across correctness hardening, fuzz/storage
  fault evidence, and performance evidence phases; no unresolved validation or
  missing delivery record changes selection priority.
- The user explicitly retained R90-75 as pending evidence and non-blocking,
  prohibited invented evidence/SLO inference, and authorized only the exact
  `v0.1.1` tag at the R90-58 candidate.
- Both tag-triggered workflows publish externally. Therefore the bounded ready
  increment is a local signed tag only; remote tag publication remains blocked.

## Validation

- Preflight exact candidate/tree, local and remote tag absence, Docker, compiler,
  Go, Make, Python, disk, signing agent, and signing-key fingerprint.
- In one isolated clean detached worktree at the candidate, run
  `VERSION=0.1.1 make rc-check` and
  `RELEASE_EVIDENCE=docs/evidence/release-v0.1.1.md make release-gate`.
- Review candidate changelog headings and release-evidence identity directly.
- Create `git tag -s v0.1.1 78cd785...` with the authorized release message.
- Verify object type, annotation, signature, exact peeled commit, local-only
  reachability, and remote absence.
- Run documentation, JSON/roadmap coverage, knowledge, diff, staged-scope, and
  sensitive-information checks before repository delivery.

## Authority Boundaries

This trigger authorizes the exact local signed Git tag and normal repository
plan/state delivery only. It does not authorize pushing the tag, GitHub Release,
GHCR, workflow dispatch, tag-trigger policy changes, a different candidate,
R90-75 evidence or SLO decisions, or any other release mutation.

## Stop Conditions

Stop on candidate/tag/signature drift, ambiguous changelog or smoke results,
any required validation failure, inability to keep the tag local, a need to
change the candidate or workflows, private data, remote tag mutation, GitHub
Release/GHCR action, or authority beyond this tag-only increment.

## Candidate Review and Validation Results

- The isolated clean detached worktree resolved to candidate
  `78cd78574e03c8f73ff68248eed2c409d6bca406`, tree
  `2f7ca21a6b4e702a80110c5c7bc268364b877150`.
- Candidate `CHANGELOG.md` SHA-256 is
  `b178bf6ba98d4aded88bca0b4400c5fc3bf86cf4e4a4e0ed6b4c5dd6d8bebf18`.
  It contains `[Unreleased]` and `[0.1.0] - Planned`, but no versioned
  `0.1.1` heading. This is recorded for the separately required changelog
  approval and remains a remote-publication blocker.
- Candidate release-evidence SHA-256 is
  `a071ca8b42750a49cd5c20b5207c7303bdba717250c56bb6d40fc63dee3f5c29`;
  it identifies `v0.1.1`, retains the approved release-gate decision, and
  still records tag/GitHub Release/GHCR outcomes as not performed.
- Fresh `VERSION=0.1.1 make rc-check` passed shell, docs, Python, evidence,
  33-test knowledge, config, dependency, full native race, 78.3% Go coverage,
  5,000-iteration ASan parser fuzz, 6-packet/5-alert E2E, archive/checksum/
  release-notes, Docker image-content, and Docker runtime-health checks.
- `RELEASE_EVIDENCE=docs/evidence/release-v0.1.1.md make release-gate` passed.
- The fresh local archive is 9,760,151 bytes with SHA-256
  `fd91e8f3afd7fbbfc5098a8a12c8a13285471b71d176ba2169d63f487509e07e`.
  This is distinct from the historical R90-58 artifact (9,760,241 bytes,
  `c68e09df...`) and is not substituted for it or authorized for publication.

## Local Tag Results

- Created signed annotated tag `v0.1.1` with tag object
  `f1a38ecb82b9c63e8411f3df040bdea84e985dd8` and message
  `NetSentry v0.1.1 release tag (local; publication pending)`.
- SSH signature verification passed for key fingerprint
  `SHA256:lanK75hksvHVuDmY55rdL1CJVqj4ZjqgBuE5/kMm4ZU` and the configured
  release identity.
- The peeled tag target is exactly
  `78cd78574e03c8f73ff68248eed2c409d6bca406` and its target type is `commit`.
- Direct `git ls-remote` checks before and after creation found no remote
  `refs/tags/v0.1.1`; no tag push, workflow dispatch, GitHub Release, or GHCR
  action occurred.

## Execution Deviations

- The first composed tag command was rejected by the local command-safety
  wrapper before execution because it included temporary-file cleanup using a
  prohibited command form. A direct local/remote recheck proved the tag still
  absent. The command was rerun without that cleanup form and the signed tag
  plus every verification passed.
- The fresh candidate archive differs from the historical R90-58 artifact by
  90 bytes and has a different digest. Because no artifact publication is
  authorized, R90-59a records both identifiers and leaves reconciliation to
  the later remote-publication gate instead of claiming equivalence.
- The first idempotent Vault replay was invoked from the Vault directory, so
  the thin hook could not resolve the repository-relative versioned sync
  script and exited before writing. Replaying the identical range from the
  repository root succeeded and preserved the manually reconciled stable MOC,
  release note, and generated iteration-note hashes.

## Delivery Results

- Documentation feature commit:
  `afb435ce8c4e708c8b7b52c5b609d1f07e232891` (`docs: record R90-59a
  local release tag`). It contains only the roadmap, plan, and task-state paths.
- The feature commit was pushed with `--no-follow-tags` and an explicit
  `main:main` refspec. A fetch verified
  `HEAD == origin/main == afb435ce8c4e708c8b7b52c5b609d1f07e232891`.
- Direct remote-tag checks after branch delivery still found no
  `refs/tags/v0.1.1`; the local signed tag object and exact candidate target
  remain unchanged.
- The post-fetch 33-test knowledge gate passed. Exact branch range
  `c19067172f1c626a59ba11b3201b276092721192..afb435ce8c4e708c8b7b52c5b609d1f07e232891`
  was synchronized idempotently to the sole local Vault.
- The generated iteration note, full index, MOC link, and stable MOC/release
  authority are verified. Stable knowledge now distinguishes the completed
  local tag from the still-unauthorized remote publication.
- R90-59a is complete. R90-59 remains blocked on changelog approval, exact
  fresh-artifact reconciliation, and explicit tag-push/GitHub Release/GHCR
  authority. R90-75 remains pending-evidence and non-blocking; no other
  increment is dependency-ready.
