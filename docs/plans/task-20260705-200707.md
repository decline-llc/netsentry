# Task Plan: atomic-commit-and-push-governance-changes

## Metadata

- Timestamp: 20260705-200707
- Branch: main
- Risk Level: Low
- Required Skill: yeet
- Inherited Policies: DEP-02, DEP-03, DEP-05, DEP-07, DEP-08, DEP-11, DEP-12, DEP-13, DEP-15, DEP-16, DEP-17, DEP-18

## Background

The user explicitly requested that differentiated task changes be committed one by one and pushed to the remote repository, following `.codex/system_prompt.md`. The current working tree contains three separable themes: local Codex ignore rules, Makefile Go cache/documentation updates, and governance planning/validation records.

## Goal

Create separate atomic commits for each task theme and push them to `origin/main`, after confirming validation and secret checks are clean.

## Scope

- Stage and commit `.gitignore` separately.
- Stage and commit Makefile cache behavior and its public docs separately.
- Stage and commit governance task plan records separately.
- Push each commit to the verified `https://github.com/decline-llc/netsentry` remote.

## Non-Goals

- No PR creation unless separately requested.
- No Docker execution.
- No core protocol, database, pcap parser, UDS protocol, SQLite schema, or API response changes.

## Files to Inspect

- `.gitignore`
- `Makefile`
- `README.md`
- `docs/development.md`
- `CHANGELOG.md`
- `docs/plans/task-20260704-223319.md`
- `docs/plans/task-20260705-200418.md`
- `docs/plans/task-20260705-200707.md`

## Files to Modify

- `docs/plans/task-20260705-200707.md`
- Git commit history

## Docs to Update

- This task plan.

## Dependency Impact

No dependency changes are planned. Existing `go mod verify` already passed after the Makefile cache change.

## Performance Impact

No runtime performance impact.

## Quality Gates

- `git diff --check`
- Secret scan over changed files
- Reuse already completed `make lint`, `go mod verify`, and elevated `make e2e-smoke` results for the same net diff.

## Execution Steps

1. Confirm remote, branch, and status.
2. Confirm changed-file secret scan remains clean.
3. Commit `.gitignore` with message `docs: ignore local codex workspace`.
4. Push commit to `origin/main`.
5. Commit Makefile cache and public docs with message `quality: use writable go build cache`.
6. Push commit to `origin/main`.
7. Commit planning/governance docs with message `docs: record governance validation`.
8. Push commit to `origin/main`.
9. Report commit hashes and push status.

## Validation Plan

No new behavior is introduced by the commit operations. The relevant net diff has already passed `make lint`, `go mod verify`, `git diff --check`, and elevated `make e2e-smoke`.

## Risk Points

- HTTPS push may fail if credentials are unavailable.
- If remote advances during the process, stop and inspect before rebasing.
- The `yeet` skill includes PR creation, but this user request only asks for commits and push; no PR will be created.

## Rollback Plan

If a commit is created but push fails, leave the local commit intact and report the authentication or network blocker. If a pushed commit must be reverted, create an explicit revert commit after user approval.

## Acceptance Criteria

- Each theme has its own commit.
- Every commit is pushed to `origin/main`.
- No sensitive material is committed.
- Docker is not invoked.

## Execution Results

- Pre-commit checks passed: `git diff --check` clean; changed-file secret scan only matched the documented `${NETSENTRY_API_TOKEN:}` placeholder.
- Commit 1 created locally: `330619e docs: ignore local codex workspace`.
- Push attempt to `origin/main` over HTTPS failed because no GitHub username/credential was available in the non-interactive environment: `fatal: could not read Username for 'https://github.com': No such device or address`.
- `gh` is not installed, so the `yeet` GitHub CLI authentication path is unavailable.
- `GITHUB_TOKEN` and `GH_TOKEN` are not present in the environment.
- After user clarification, `origin` was restored to `git@github.com:decline-llc/netsentry.git`.
- Commit 1 pushed over SSH: `330619e docs: ignore local codex workspace`.
- Commit 2 created and pushed over SSH: `79c1cd7 quality: use writable go build cache`.
- Commit 3 created for governance task records: `c8ac385 docs: record governance validation`; amended only to record its own commit hash before push.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| yeet | Partially applied | Read skill and applied commit/push discipline. PR creation skipped because the user did not request a PR. | Direct git commit/push commands with explicit status reporting. |
