# Task Plan: R90-05 expired-exception preflight

## Metadata

- Timestamp: 2026-07-16T09:06:41Z
- Branch: main
- Risk Level: High
- Remote baseline: `673a5d580a08db44de76aa17ef6301e4e48f402a`

## Goal

Fail closed if a release-gate caller attempts to reuse the R90-04-only public
traffic exception after R90-04 completion.

## Scope

- Mark the R90-04 audit exception expired at its verified completion commit.
- Make the release gate reject that exception for release approval while
  retaining its field-level validation.
- Add a direct regression proving otherwise-valid R90-04 evidence cannot pass
  the release gate.
- Reconcile roadmap and release documentation with the enforced boundary.

## Non-Goals

- Do not start R90-05 validation before its 2026-09-12 window.
- Do not create v0.1.1 evidence, approve a release, tag, publish, or alter the
  immutable v0.1.0 evidence decision.
- Do not remove the historical R90-04 evidence or exception record.

## Risk

Without an executable expiry check, a caller can supply the R90-04 exception
and a release-shaped evidence document to satisfy `release_gate.validate`,
contradicting the audit record and roadmap boundary.

## Validation

- Focused release-gate tests must prove the expired R90-04 exception is rejected
  and the historical v0.1.0 exception remains valid.
- Run Python, documentation, knowledge, and diff checks.
- Review the staged paths for credentials, private traffic paths, and accidental
  release claims before commit.

## Acceptance Criteria

- The R90-04 exception records its expired state and completion commit.
- `release_gate.validate` returns a specific failure for an otherwise-valid
  R90-04-backed release record.
- Existing distinct R90-04 rejection tests and the v0.1.0 passing fixture remain
  green.
- The focused commit is pushed, fetched back as `origin/main`, and synchronized
  to the local Vault using the exact pushed range.

## Stop Conditions

Stop if enforcement would invalidate the historical v0.1.0 release evidence,
requires a new release-policy decision, or produces an ambiguous release-gate
result.
