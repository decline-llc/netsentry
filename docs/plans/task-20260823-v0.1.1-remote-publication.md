# Task Plan: R90-59 remote v0.1.1 publication

## Metadata

- Timestamp: 2026-08-23T08:59:49-07:00
- Branch: main
- Risk Level: High
- Remote baseline: `8724b816a77c4bdeac899e4848dcb5bcd5232a93`
- Candidate: `78cd78574e03c8f73ff68248eed2c409d6bca406`
- Tag object: `f1a38ecb82b9c63e8411f3df040bdea84e985dd8`
- Tag: `v0.1.1`

## Goal

Publish the existing signed `v0.1.1` tag at the exact validated R90-58
candidate, then directly verify the tag-triggered GitHub Release assets and
GHCR image identities before recording R90-59 as complete.

## Scope

- Reconcile the user's explicit publication, changelog, and fresh-artifact
  authorization with the exact local tag object and candidate.
- Revalidate the immutable tag signature, target, candidate workflow inputs,
  release evidence, changelog, and release gates before external mutation.
- Push only `refs/tags/v0.1.1` without moving the tag or changing `main`.
- Observe both tag-triggered workflows to a terminal successful result.
- Verify the remote tag object and peeled candidate, GitHub Release identity,
  release asset names/checksum/bytes/platform, and GHCR `v0.1.1` plus `0.1.1`
  tag digests/platforms.
- Reconcile roadmap, task state, public release evidence, release-readiness
  prose, fetched remote facts, and exact-range local Vault evidence.

## Non-Goals

- Do not move, recreate, resign, or retarget `v0.1.1`.
- Do not change the historical candidate, workflows, runtime, protocol,
  configuration, dependency, release-gate, or artifact-generation behavior.
- Do not dispatch replacement workflows or publish extra tags, assets, images,
  platforms, or a different version unless recovery from a directly verified
  workflow failure is separately authorized.
- Do not rewrite immutable historical iteration notes or historical artifact
  identifiers; record the workflow-produced publication artifact distinctly.
- Do not create comparable-environment benchmark evidence, choose a
  performance budget, or start R90-75.

## Risks

- A tag push is an externally visible, effectively irreversible publication
  trigger; the exact object and candidate must be proven before push.
- The historical candidate changelog keeps v0.1.1 content under
  `[Unreleased]`; the user explicitly approved publishing that immutable
  payload, so current documentation must record the exception without
  pretending the tag contains a version heading.
- Prior clean candidate builds produced two different archive byte counts and
  digests. The workflow-produced archive and its paired checksum are the only
  publication artifact identity; neither earlier local artifact may be
  substituted or described as identical.
- GitHub API access showed one transient TLS handshake timeout during the
  trigger audit. Publication must not begin until authenticated API access is
  reliable enough to verify both workflows and external artifacts.
- A successful tag push does not prove either workflow, Release, asset, or
  image outcome; every external result requires direct verification.

## Acceptance Criteria and Evidence Map

| Acceptance criterion | Planned evidence |
| --- | --- |
| Exact authority and immutable tag | User authorization; local tag object, SSH signature, annotation, and peeled target verification |
| Historical candidate approved as-is | Direct candidate changelog/release-evidence review and recorded user approval without tag movement |
| Candidate remains publishable | Isolated clean candidate RC, fetched supply-chain audit, release gate, archive/checksum/platform reconciliation |
| Exact remote tag | Fresh remote ref fetch; remote tag object equals `f1a38ec...` and peels to `78cd785...` |
| GitHub Release is complete | Successful exact-SHA Release workflow; published non-draft/non-prerelease release; expected archive/checksum assets with verified checksum |
| GHCR publication is complete | Successful exact-SHA Docker Publish workflow; `v0.1.1` and `0.1.1` resolve to the same directly inspected digest and expected platform |
| Delivery records are recoverable | Roadmap/task/release documentation, exact repository commits and fetched `origin/main`, exact Vault notes/index/MOC, stable release-knowledge reconciliation |

## Validation

- Fetch `origin/main` and tags; require a clean
  `FETCH_HEAD == HEAD == origin/main` baseline and remote tag absence.
- Revalidate local annotated tag object, SSH signer, annotation, target type,
  exact peeled candidate, and ancestry from current `origin/main`.
- Inspect the workflows, release evidence, and changelog from the tag payload,
  not from current `main`.
- Preflight every repository-pinned tool and run, from an isolated clean
  candidate worktree, `VERSION=0.1.1 make rc-check`,
  `SUPPLY_CHAIN_FETCH_ASSETS=1 make supply-chain-check`, and
  `RELEASE_EVIDENCE=docs/evidence/release-v0.1.1.md make release-gate` in one
  fail-fast sequence.
- Independently verify the generated archive filename, platform, byte count,
  SHA-256 file, checksum, contents, and embedded release-note version.
- Push only the exact tag ref; fetch it back and compare the full tag object
  and peeled candidate.
- Wait for both tag workflows without fixed sleeps beyond bounded polling;
  inspect terminal conclusions and failed logs before any recovery decision.
- Query and download the GitHub Release assets, verify the checksum file
  against the archive, and compare the workflow/tag/commit/platform identities.
- Inspect both GHCR semantic tags and require equal digest plus the expected
  published platform.
- Before repository delivery, run applicable documentation/evidence/JSON
  checks, complete roadmap multiset validation, `make knowledge-check`,
  `git diff --check`, exact staged-scope review, and anchored sensitive-data
  review.
- Push `main` without force or tags, fetch and require exact ref equality,
  rerun `make knowledge-check`, then synchronize and verify each exact full-SHA
  branch range in the sole local Vault.

## Authority Boundaries

The user explicitly authorizes pushing the existing signed `v0.1.1` tag object
`f1a38ecb82b9c63e8411f3df040bdea84e985dd8`, which peels to candidate
`78cd78574e03c8f73ff68248eed2c409d6bca406`, and authorizes its checked-in
tag-triggered GitHub Release and GHCR workflows. The user also approves the
historical `[Unreleased]` changelog shape and reconciliation of the exact
workflow-produced artifact as distinct from both prior local builds. This does
not authorize moving the tag, changing the candidate or workflows, dispatching
replacement publication, publishing another version/platform, deleting or
rewriting an external artifact, or starting R90-75.

## Trigger Audit

- Read the `netsentry-next` and `netsentry-roadmap` skills, active roadmap,
  knowledge contract, R90-58/R90-59a plans and states, exact tag workflows,
  candidate changelog/evidence, and current stable Vault authority.
- Fetched `origin/main` and tags and verified a clean
  `FETCH_HEAD == HEAD == origin/main == 8724b816a77c4bdeac899e4848dcb5bcd5232a93`
  baseline with no remote `v0.1.1` tag or GitHub Release.
- Verified the local annotated tag object remains
  `f1a38ecb82b9c63e8411f3df040bdea84e985dd8`, peels exactly to
  `78cd78574e03c8f73ff68248eed2c409d6bca406`, and is an ancestor of current
  `origin/main`.
- Verified R90-104 feature/closure Git scopes, completed state, both immutable
  Vault notes, full-index rows, MOC links, current stable authority, and the
  passing 33-test knowledge gate.
- Reviewed 181 Jul 20 through Aug 22 commits across four phases: 59 commits Jul
  20-25, 40 Jul 26-Aug 2, 46 Aug 3-9, and 36 Aug 10-22. No missing delivery
  record, stale stable authority, or unresolved local validation result changes
  priority.
- Parsed all 119 prior task states and verified all 108 roadmap rows and
  Definitions match as complete multisets without duplicates or asymmetry.
- The user's explicit follow-up grants the exact R90-59 tag push, GitHub
  Release, GHCR, changelog, and fresh-artifact reconciliation authority.
  R90-59 is therefore selected; R90-75 retains its independent evidence and
  product/SLO blocker.
- Authenticated GitHub identity lookup succeeded, while one workflow-list API
  call hit a TLS handshake timeout. API reliability must be re-established
  before the irreversible tag push.

## Stop Conditions

Stop before tag push on any tag object, signature, candidate, ancestry,
workflow, changelog-approval, evidence, archive, checksum, platform, remote,
credential, or validation ambiguity. After tag push, do not delete, retarget,
overwrite, rerun, or replace external publication on failure without direct
evidence and new recovery authority. Stop before repository delivery if the
Release asset/checksum, GHCR digest/platform, workflow, remote, or Vault result
cannot be verified exactly.

## Pre-Publication Validation Blocker

- The exact candidate's structural supply-chain policy and actionlint checks
  pass with the repository-pinned tools built from their already verified
  module-cache sources under Go 1.25.12.
- The subsequent pinned `govulncheck v1.6.0` scan fails with three reachable
  standard-library findings in Go 1.25.12:
  `GO-2026-6090` in `crypto/tls`, `GO-2026-6089` in `net/http`, and
  `GO-2026-5972` in `encoding/asn1`. The current vulnerability database names
  Go 1.25.13 as the fix for every finding.
- The last three `main` GitHub Actions runs also fail at `Run supply-chain
  checks`, including R90-104 feature and closure runs. The local exact-candidate
  result establishes the same failure boundary without promoting those job-
  level records into unsupported exact finding claims.
- Both tag workflows install the same pinned scanner and run
  `make supply-chain-check` before GitHub Release or GHCR publication.
  Therefore pushing the tag would knowingly trigger two failed workflows and
  cannot satisfy R90-59 acceptance.
- The remote tag and GitHub Release remain absent. No tag push, workflow
  dispatch, Release mutation, GHCR publication, or repository push occurred.

## Required Unblock Authority

Safe completion now requires a new candidate containing a reviewed Go 1.25.14
toolchain/supply-chain-lock update, clean candidate and publication-gate
validation, fresh artifact identity, and explicit authority to replace and
resign the still-local `v0.1.1` tag at that new candidate. The current grant
does not authorize moving or recreating the exact tag, so R90-59 stops before
external mutation.

The subsequent `$netsentry-next` trigger selected R90-105 to deliver the
current-`main` Go 1.25.14 toolchain hardening as the smallest local unblocker.
This R90-59 plan/state may be committed and Vault-synchronized with R90-105 as
dependency/blocker evidence, but that delivery does not complete R90-59 or
authorize candidate-tag replacement or publication.
