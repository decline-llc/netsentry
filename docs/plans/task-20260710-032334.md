# Task Plan: reconcile persisted release state

## Metadata

- Timestamp: 20260710-032334
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: ENV-01, DOC-01, DOC-02, QUALITY-03, DEPS-01, SKILL-01

## Background

The persisted task state from 20260708 still says the release workflow/documentation commits are pending push. The current Git state shows `main` and `origin/main` are synchronized, and the latest commits are already present on the approved SSH remote. Public release readiness docs mostly reflect this, but the development guide still has stale wording that can imply a passing release commit has not been pushed.

## Goal

Align persisted release task state and public release documentation with the verified Git state: release commits are pushed, while external fuzz evidence, realistic sanitized pcap corpus evidence, and tag-driven publication verification remain pending before v0.1.0.

## Scope

- Update stale release blocker wording in `docs/development.md`.
- Update the persisted task state file so it no longer reports a pending push.
- Add this planning document for traceability.

## Non-Goals

- Do not create or push the `v0.1.0` tag.
- Do not publish a GitHub Release or GHCR image.
- Do not fabricate or commit fuzz or pcap corpus evidence.
- Do not change runtime code, APIs, configuration, workflows, or dependencies.

## Files to Inspect

- `docs/tasks/task-state-20260708-051113.json`
- `docs/development.md`
- `docs/release-readiness.md`
- `README.md`
- Git branch and remote state

## Files to Modify

- `docs/tasks/task-state-20260708-051113.json`
- `docs/development.md`
- `docs/plans/task-20260710-032334.md`

## Docs to Update

- `docs/development.md`
- This task plan
- Existing persisted task state

## Dependency Impact

No dependency changes are expected. `go.mod`, `go.sum`, `engine/go.mod`, and `engine/go.sum` must remain unchanged.

## Performance Impact

No runtime or release workflow performance impact.

## Quality Gates

- `make docs-check`
- `git diff --check`
- Sensitive keyword scan over the staged diff
- Dependency file diff check

## Execution Steps

1. Verify local branch, approved SSH remote, and `origin/main...HEAD` sync state.
2. Update development documentation to remove stale pushed-commit blocker wording.
3. Update the persisted task state to show the push checkpoint is complete and the remaining blockers are evidence/tag verification.
4. Run documentation, whitespace, dependency, and sensitive-output checks.
5. Commit and push a single documentation/state reconciliation change if validation passes.

## Validation Plan

Run `git status --short --branch`, `git rev-list --left-right --count origin/main...HEAD`, `make docs-check`, `git diff --check`, a dependency diff check for module files, and a sensitive keyword scan over the diff.

## Risk Points

- Accidentally implying v0.1.0 is already tagged or published.
- Hiding the remaining external fuzz and realistic corpus evidence gates.
- Updating state without preserving the historical release validation details.

## Rollback Plan

Revert the documentation/state reconciliation commit if release owners prefer the previous wording or state representation.

## Acceptance Criteria

- Persisted task state no longer lists pushing release workflow commits as pending.
- Development docs match README and release-readiness docs on remaining pre-tag blockers.
- Validation commands pass.
- A single-topic commit is pushed to `git@github.com:decline-llc/netsentry.git`.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Defined a measurable state reconciliation goal with explicit validation and non-goals. | None |
