# Task Plan: sync release evidence task state

## Metadata

- Timestamp: 20260710-032913
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: ENV-01, DOC-01, DOC-02, QUALITY-03, DEPS-01, SKILL-01, no-sensitive-output

## Background

The v0.1.0 release-gate task is tracked in `docs/tasks/task-state-20260708-051113.json`. Commit `58480ab` added the public release evidence guidance and template, which is a completed support step for the remaining external fuzz, realistic pcap corpus, and tag publication gates. The persisted task state should include this completed step so future sessions resume from the current project state.

## Goal

Update the persisted release-gate task state to record that the public sanitized release evidence template is available, while keeping the remaining release blockers unchanged until real evidence and tag workflow verification are complete.

## Scope

- Add this planning document.
- Update the existing release task state completed steps and checkpoint.
- Preserve the pending external evidence and tag publication verification blockers.

## Non-Goals

- Do not mark sustained external fuzz evidence complete.
- Do not mark realistic sanitized pcap corpus evidence complete.
- Do not create or push the `v0.1.0` tag.
- Do not change runtime code, tests, APIs, configs, CI workflows, dependencies, or release artifacts.

## Files to Inspect

- `docs/tasks/task-state-20260708-051113.json`
- `docs/evidence/README.md`
- `docs/evidence/release-evidence-template.md`
- Git branch and remote state

## Files to Modify

- `docs/tasks/task-state-20260708-051113.json`
- `docs/plans/task-20260710-032913.md`

## Docs to Update

- Existing persisted task state.
- This task plan.

## Dependency Impact

No dependency changes are expected. `go.mod`, `go.sum`, `engine/go.mod`, and `engine/go.sum` must remain unchanged.

## Performance Impact

No runtime, validation, or release workflow performance impact.

## Quality Gates

- `make docs-check`
- `git diff --check`
- `make deps-check`
- Staged diff sensitive keyword scan
- Dependency file diff check

## Execution Steps

1. Confirm the repository is synchronized with `origin/main`.
2. Record the public release evidence template as a completed support step in the persisted task state.
3. Keep the pending blockers focused on external fuzz evidence, realistic sanitized pcap corpus evidence, and tag publication verification.
4. Run documentation, dependency, whitespace, and sensitive-output checks.
5. Commit and push the state-only documentation update.

## Validation Plan

Run `make docs-check`, `git diff --check`, `make deps-check`, inspect staged files, confirm dependency files are unchanged, scan the staged diff for secret patterns, and verify `main...origin/main` is `0 0` after push.

## Risk Points

- Accidentally implying release evidence is complete when only the template is complete.
- Accidentally changing pending release gates.
- Creating noisy state churn unrelated to the current release task.

## Rollback Plan

Revert the state synchronization commit if release owners prefer not to track the evidence-template support step in the persisted task state.

## Acceptance Criteria

- The persisted task state includes the public evidence template as a completed support step.
- The remaining release blockers still require real external fuzz evidence, realistic sanitized pcap evidence, and tag publication verification.
- Validation commands pass.
- A single documentation/state commit is pushed to `git@github.com:decline-llc/netsentry.git`.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Used | Defined a measurable state synchronization goal with explicit non-goals and validation criteria. | None |
