# Task Plan: verify release corpus prerequisites

## Metadata

- Timestamp: 2026-07-10T21:00:45-07:00
- Branch: main
- Risk Level: High
- Required Skill: define-goal
- Inherited Policies: DEP-03, DEP-04, DEP-05, DEP-06, DEP-08, DEP-10, DEP-12, DEP-13, DEP-16, DEP-17, DEP-18, DEP-19

## Background

The v0.1.0 release gate still requires reviewed large-scale external fuzz and realistic sanitized pcap pressure/query evidence. The workspace contains only deterministic synthetic NetSentry samples. Their manifests explicitly identify synthetic payloads and documentation-only addresses, so they cannot be represented as production traffic evidence.

## Goal

Complete stage 1 inventory and provenance checks, preserve the release blocker accurately, and define the exact input needed to resume stage 2 without fabricating evidence or using an unapproved permanent exception.

## Scope

- Inspect repository, TEST workspace, and local temporary corpus candidates.
- Verify manifest claims, SHA-256 identity, file count, and synthetic/real classification.
- Run the existing `make release-gate` only to record its expected missing-evidence failure.
- Persist the blocker and recovery instructions.

## Non-Goals

- No production or core code changes.
- No synthetic corpus inflation presented as real traffic evidence.
- No `make e2e-corpus-pressure` release evidence run without approved corpus provenance.
- No permanent exception, tag, GitHub Release, or GHCR publication.

## Files to Inspect

- `docs/release-readiness.md`
- `docs/evidence/release-evidence-template.md`
- `scripts/gen_sanitized_corpus.py`
- `scripts/e2e_corpus_pressure.sh`
- `docs/tasks/task-state-20260711-035222.json`

## Files to Modify

- This plan.
- `docs/tasks/task-state-20260711-040045.json`.

## Docs to Update

- Persisted task state only; public release-readiness wording already preserves the blocker.

## Dependency Impact

None.

## Performance Impact

None. No release pressure campaign is accepted without an approved corpus.

## Quality Gates

- Repository/remote/tag checks.
- Manifest and SHA-256 inspection.
- Expected `make release-gate` failure due to missing reviewed evidence.
- `git diff --check` and secret/path review.

## Execution Steps

1. Confirm clean main branch and approved SSH origin.
2. Inventory available pcap/pcapng and manifests.
3. Classify the available inputs and confirm whether they are production-derived.
4. Persist the blocker and exact resume condition.
5. Do not commit or push raw corpus/evidence files.

## Validation Plan

Stage 1 passes only as a prerequisite audit when no unverified input is treated as release evidence, synthetic samples are explicitly classified, and the next action requires either operator-supplied corpus provenance or a separately recorded authorized exception.

## Risk Points

- Treating deterministic synthetic samples as realistic production traffic.
- Committing private corpus paths or raw packet data.
- Bypassing the release gate without explicit administrator approval.

## Rollback Plan

Remove only this plan/state pair if the release owner supplies a new approved corpus workflow. Do not modify runtime code or release artifacts.

## Acceptance Criteria

- No production-derived corpus is falsely claimed to exist.
- Synthetic corpus hashes and manifest classification are recorded.
- `make release-gate` remains blocked for the correct reason.
- Resume instructions identify the required corpus metadata and next commands.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Success | Converted the staged release plan into a bounded prerequisite audit with a safe stop condition. | None |
