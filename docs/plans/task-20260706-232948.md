# Task Plan: run long parser fuzz pass

## Metadata

- Timestamp: 20260706-232948
- Branch: main
- Risk Level: Low
- Required Skill: define-goal
- Inherited Policies: native Makefile, v0.1.0 boundary, docs sync, dependency check, quality gate, sensitive-info scan

## Background

The v0.1.0 release readiness checklist still requires sustained external C fuzz evidence against larger parser and formatter corpora. The repository currently has deterministic ASan parser fuzz smoke in `make fuzz-parser` and a longer configurable `make fuzz-parser-long` target. A long deterministic ASan pass does not replace external corpus fuzzing, but it provides a useful local evidence point and keeps release readiness moving without changing runtime behavior.

## Goal

Run `FUZZ_LONG_ITERATIONS=1000000 make fuzz-parser-long` and record whether the current C frame parser completes one million deterministic ASan fuzz iterations with zero crash.

## Scope

- Run the existing long C parser fuzz target with one million iterations.
- Record results in this plan and the ignored local `DEVLOG.md`.
- Keep the external larger-corpus fuzz blocker open.

## Non-Goals

- No C parser or fuzz harness code changes.
- No new corpus files.
- No Docker execution.
- No release tag or publication.

## Files to Inspect

- `capture/tests/fuzz_parser.c`
- `capture/Makefile`
- `Makefile`
- `docs/development.md`
- `DEVLOG.md`

## Files to Modify

- `docs/plans/task-20260706-232948.md`
- `DEVLOG.md` (ignored local state file)

## Docs to Update

- This task plan.
- Local `DEVLOG.md` validation record.

## Dependency Impact

No dependency changes. Run `make deps-check` to verify current Go module integrity.

## Performance Impact

No runtime behavior changes. The fuzz command is CPU-bound validation work and may take longer than normal smoke checks.

## Quality Gates

- `FUZZ_LONG_ITERATIONS=1000000 make fuzz-parser-long`
- `git diff --check`
- `make deps-check`
- `make lint`

## Execution Steps

1. Confirm the working tree is clean except ignored local `DEVLOG.md`.
2. Run the long deterministic ASan parser fuzz target with one million iterations.
3. Record the result and explicitly note that external corpus fuzzing remains pending.
4. Run lightweight validation and sensitive-info scan.
5. Commit and push the tracked validation plan.

## Validation Plan

Primary validation is `FUZZ_LONG_ITERATIONS=1000000 make fuzz-parser-long`. Supporting validation is `git diff --check`, `make deps-check`, `make lint`, and a sensitive-info scan over the changed docs.

## Risk Points

- ASan or local environment issues may cause a validation failure unrelated to parser logic.
- This pass is deterministic and built-in seed based; it does not close the external corpus fuzz blocker.
- Do not overstate this result as a sustained external fuzz campaign.

## Rollback Plan

If validation cannot run, record the blocker in this plan. If the plan should not be committed, delete this file. No source rollback is expected.

## Acceptance Criteria

- One million deterministic ASan fuzz iterations pass, or a concrete blocker is recorded.
- External larger-corpus fuzz remains documented as pending.
- No source code or dependency changes are made.
- Validation record is committed and pushed.

## Validation Result

- `FUZZ_LONG_ITERATIONS=1000000 make fuzz-parser-long`: passed.
- Fuzz output: `fuzz_parser: ok iterations=1000000`.
- This result covers the existing deterministic ASan mutation harness seeded with built-in Ethernet/IP/TCP/UDP, VLAN, Q-in-Q, fragment, short-frame, malformed TCP-offset, and empty-frame cases.
- This does not close the sustained external larger-corpus fuzz blocker; it is recorded as an intermediate release-readiness evidence point.

## Security Check

No key, token, private key, sudo password, production secret, source code, dependency, or Docker change was added by this validation task.

## Skill Invocation Log

| Skill | Status | Result | Fallback |
|---|---|---|---|
| define-goal | Applied | Goal narrowed to a measurable one-million-iteration deterministic ASan fuzz pass that does not claim to satisfy external corpus fuzzing. | Not needed |
